TOPIC
    about_DRMMSite

SHORT DESCRIPTION
    Describes the DRMMSite class and its methods for managing sites in Datto RMM.

LONG DESCRIPTION
    The DRMMSite class represents a site (customer) within Datto RMM. Sites are organisational containers that hold devices, filters, variables, and settings. 
 
DRMMSite objects are returned by Get-RMMSite and provide a rich object-oriented interface for interacting with site resources through instance methods.

PROPERTIES (from class)
    Id [long]
    Uid [string]
    AccountUid [string]
    Name [string]
    Description [string]
    Notes [string]
    OnDemand [bool]
    SplashtopAutoInstall [bool]
    ProxySettings [DRMMSiteProxySettings]
    DevicesStatus [DRMMDevicesStatus]
    SiteSettings [DRMMSiteSettings]
    Variables [DRMMVariable[]]
    Filters [object]
    AutotaskCompanyName [string]
    AutotaskCompanyId [string]
    PortalUrl [string]

METHODS (from class)
    GetSummary() string
    Set([hashtable]$Properties) DRMMSite
    OpenPortal() void
    GetDeviceCount() int
    GetVariable([string]$Name) DRMMVariable
    NewVariable([string]$Name, [string]$Value) DRMMVariable
    NewVariable([string]$Name, [string]$Value, [bool]$Masked) DRMMVariable
    GetFilter([string]$Name) DRMMFilter
    GetSettings() DRMMSiteSettings
    SetProxy([string]$ProxyHost, [int]$Port, [string]$Type) DRMMSiteSettings
    SetProxy([string]$ProxyHost, [int]$Port, [string]$Type, [string]$Username, [SecureString]$Password) DRMMSiteSettings
    RemoveProxy() DRMMSiteSettings

SEE ALSO
    Get-RMMSite
    Get-RMMSiteSettings
    Set-RMMSiteProxy
    Remove-RMMSiteProxy
    Get-RMMDevice
    Get-RMMDeviceFilter
    Get-RMMVariable
    New-RMMVariable
    Get-RMMAlert
    about_DRMMDevice
    about_DRMMFilter
    about_DRMMAlert

