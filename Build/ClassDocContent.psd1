@{
    _Metadata = @{
        AutoGenerated = $true
        Description = 'Documentation content for DattoRMM.Core classes'
        LastUpdated = '2026-03-07'
        Version = '1.0.0'
    }
    DRMMAccount = @{
        DRMMAccount = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAccount class models an account within the DRMM platform, encapsulating properties such as the account ID, unique identifier, name, currency, and related descriptors and device status. It provides a static method to create an instance of the class from a typical API response object that contains account information. The class also includes a method to generate a summary string that combines the account name with its device status for easy display. The DRMMAccountDescriptor and DRMMAccountDevicesStatus classes represent related information about the account, such as billing details and device status, respectively.'
            MethodDescriptions = @{
                GetSummary = @{
                    Description = 'Generates a summary string for the account, including its name and device status.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string combining the account name and device status.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                Currency = 'The currency associated with the account, typically represented as a three-letter ISO currency code.'
                Descriptor = 'An instance of the DRMMAccountDescriptor class that provides additional details about the account, such as billing email, device limit, and time zone.'
                DevicesStatus = 'An instance of the DRMMAccountDevicesStatus class that provides information about the number of devices associated with the account and their status (online, offline, on-demand, managed).'
                Id = 'The unique identifier for the account.'
                Name = 'The name of the account.'
                Uid = 'The unique identifier string for the account.'
            }
            RelatedLinks = @(
                'DRMMAccountDescriptor'
                'DRMMAccountDevicesStatus'
                'Get-RMMAccount'
            )
            ShortDescription = 'Represents an account in the DRMM system, including its properties and related information.'
        }
        DRMMAccountDescriptor = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAccountDescriptor class encapsulates details about a DRMM account''s billing email, device limit, and time zone. It provides a static method to create an instance of the class from a typical API response object that contains these descriptor details. This class is used as a property within the DRMMAccount class to provide additional information about the account''s configuration and limitations.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                BillingEmail = 'The billing email address associated with the account.'
                DeviceLimit = 'The maximum number of devices allowed for the account.'
                TimeZone = 'The time zone setting for the account.'
            }
            RelatedLinks = @(
                'DRMMAccount'
                'Get-RMMAccount'
            )
            ShortDescription = 'Represents the descriptor information for a DRMM account, including billing and timezone details.'
        }
        DRMMAccountDevicesStatus = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAccountDevicesStatus class encapsulates information about the number of devices associated with a DRMM account, including the total number of devices, the number of online devices, offline devices, on-demand devices, and managed devices. It provides a static method to create an instance of the class from a typical API response object that contains these device status details. The class also includes methods to calculate the percentage of online devices and to generate a summary string that combines this information for easy display. This class is used as a property within the DRMMAccount class to provide insights into the account''s device status.'
            MethodDescriptions = @{
                GetOnlinePercentage = @{
                    Description = 'Calculates the percentage of online devices for the account.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'The percentage of online devices as a double value.'
                }
                GetSummary = @{
                    Description = 'Generates a summary string for the device status, including the count of online devices and total devices.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string combining the count of online devices and total devices.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                NumberOfDevices = 'The total number of devices associated with the account.'
                NumberOfManagedDevices = 'The number of devices that are managed within the account.'
                NumberOfOfflineDevices = 'The number of devices that are currently offline.'
                NumberOfOnDemandDevices = 'The number of devices that are on-demand.'
                NumberOfOnlineDevices = 'The number of devices that are currently online.'
            }
            RelatedLinks = @(
                'DRMMAccount'
                'Get-RMMAccount'
            )
            ShortDescription = 'Represents the device status information for a DRMM account, including counts of devices in various states.'
        }
    }
    DRMMActivityLog = @{
        DRMMActivityLog = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLog class models an activity log entry within the DRMM platform, encapsulating properties such as the log ID, entity, category, action, date, site information, device ID, hostname, user information, activity details, and flags indicating the presence of standard output and error. It provides a static method to create an instance of the class from a typical API response object that contains activity log information. The class also includes a method to generate a summary string that combines key properties of the activity log for easy display. The related classes DRMMActivityLogSite and DRMMActivityLogUser represent nested information about the site and user associated with the activity log entry.'
            MethodDescriptions = @{
                GetSummary = @{
                    Description = 'Generates a summary string for the activity log entry, including key details about the activity.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string combining key details of the activity log entry.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                Action = 'The action performed in the activity log entry.'
                Category = 'The category of the activity log entry.'
                Date = 'The date and time when the activity occurred.'
                Details = 'Additional details about the activity.'
                DeviceId = 'The identifier of the device involved in the activity.'
                Entity = 'The entity associated with the activity.'
                HasStdErr = 'Indicates whether the activity log entry includes standard error output.'
                HasStdOut = 'Indicates whether the activity log entry includes standard output.'
                Hostname = 'The hostname of the device involved in the activity.'
                Id = 'The unique identifier for the activity log entry.'
                Site = 'An instance of the DRMMActivityLogSite class that provides information about the site associated with the activity log entry.'
                User = 'An instance of the DRMMActivityLogUser class that provides information about the user associated with the activity log entry.'
            }
            RelatedLinks = @(
                'DRMMActivityLogSite'
                'DRMMActivityLogUser'
                'Get-RMMActivityLog'
            )
            ShortDescription = 'Represents an activity log entry in the DRMM system, including details about the activity, associated site and user information, and related context.'
        }
        DRMMActivityLogDetails = @{
            Examples = @{
            }
            LongDescription = 'The ''Details'' property of a DRMMActivityLog entry is designed to hold additional information about the activity in a flexible format.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the ''Details'' Property of a DRMMActivityLog entry, which can contain arbitrary key-value pairs with additional information about the activity.'
        }
        DRMMActivityLogDetailsDeviceDevice = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLogDetailsDeviceDevice class serves as a base class for DEVICE entity device category activity logs. It encapsulates properties that are common across different device actions (move, etc.), including source forwarding information, in addition to the entity-level DEVICE properties inherited from DRMMActivityLogEntityDevice. Specific device action types inherit from this class and add their unique properties.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                SourceForwardedIp = 'The forwarded IP address of the source that initiated the device activity.'
            }
            RelatedLinks = @()
            ShortDescription = 'Base class for DEVICE device-related activity log details, containing properties common to all device actions.'
        }
        DRMMActivityLogDetailsDeviceDeviceGeneric = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLogDetailsDeviceDeviceGeneric class is used for DEVICE entity device category activity logs where the specific action is not yet mapped to a dedicated class. It inherits the 7 base properties common to all DEVICE device activities and dynamically adds any additional properties found in the response that are not part of the base class. This ensures type safety for known properties while maintaining flexibility for unknown actions.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a generic DEVICE device activity log details for unknown device actions, with base properties and dynamic additional properties.'
        }
        DRMMActivityLogDetailsDeviceDeviceMoveDevice = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLogDetailsDeviceDeviceMoveDevice class models the details of a device site movement activity log entry. It inherits common device properties from DRMMActivityLogDetailsDeviceDevice and adds movement-specific properties including source and destination site information (IDs, names, UIDs), site name, and user information (email, first name, last name, username, user ID) related to the device move operation.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                DataFromSiteId = 'The identifier of the site the device was moved from.'
                DataFromSiteName = 'The name of the site the device was moved from.'
                DataFromSiteUid = 'The unique identifier (UID) of the site the device was moved from.'
                DataToSiteId = 'The identifier of the site the device was moved to.'
                DataToSiteName = 'The name of the site the device was moved to.'
                DataToSiteUid = 'The unique identifier (UID) of the site the device was moved to.'
                SiteName = 'The name of the site associated with the device move operation.'
                UserEmail = 'The email address of the user who performed the device move.'
                UserFirstName = 'The first name of the user who performed the device move.'
                UserId = 'The identifier of the user who performed the device move.'
                UserLastName = 'The last name of the user who performed the device move.'
                UserUsername = 'The username of the user who performed the device move.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents an activity log of entity DEVICE, category device, and action move'
        }
        DRMMActivityLogDetailsDeviceGeneric = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLogDetailsDeviceGeneric class is used for DEVICE entity activity logs where the category is not yet mapped to a dedicated class (not job, remote, or device). It inherits the 6 base properties common to all DEVICE activities and dynamically adds any additional properties found in the response. This ensures type safety for known entity-level properties while maintaining flexibility for unknown categories.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a generic DEVICE entity activity log for unknown categories, with entity-level properties and dynamic additional properties.'
        }
        DRMMActivityLogDetailsDeviceJob = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLogDetailsDeviceJob class serves as a base class for DEVICE entity job category activity logs. It encapsulates properties that are common across different job actions (deployment, create, etc.), including job identifiers and site information, in addition to the entity-level DEVICE properties inherited from DRMMActivityLogEntityDevice. Specific job action types inherit from this class and add their unique properties.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                JobId = 'The numeric identifier of the job associated with the activity.'
                JobName = 'The name of the job associated with the activity.'
                JobStatus = 'The status of the job at the time of the activity (e.g., completed, failed).'
                JobUid = 'The unique identifier (UID) of the job associated with the activity.'
                SiteName = 'The name of the site where the job was executed.'
            }
            RelatedLinks = @()
            ShortDescription = 'Base class for DEVICE job-related activity log details, containing properties common to all job actions.'
        }
        DRMMActivityLogDetailsDeviceJobCreate = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLogDetailsDeviceJobCreate class models the details of a job creation activity log entry. It inherits common job properties from DRMMActivityLogDetailsDeviceJob and adds creation-specific properties such as the job creation date and user information (email, first name, last name, username, user ID).'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                JobDateCreated = 'The date and time when the job was created.'
                UserEmail = 'The email address of the user who created the job.'
                UserFirstName = 'The first name of the user who created the job.'
                UserId = 'The identifier of the user who created the job.'
                UserLastName = 'The last name of the user who created the job.'
                UserUsername = 'The username of the user who created the job.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents an activity log of entity DEVICE, category job, and action create, which includes specific properties related to job creation activities.'
        }
        DRMMActivityLogDetailsDeviceJobDeployment = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLogDetailsDeviceJobDeployment class models the details of a job deployment activity log entry. It inherits common job properties from DRMMActivityLogDetailsDeviceJob and adds deployment-specific properties such as deployment ID, scheduled job information, and notes.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                JobDeploymentId = 'The identifier of the job deployment.'
                JobScheduledJobId = 'The identifier of the scheduled job associated with the deployment.'
                JobScheduledJobUid = 'The unique identifier (UID) of the scheduled job associated with the deployment.'
                Note = 'An optional note or comment associated with the job deployment.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents an activity log of entity DEVICE, category job, and action deployment, which includes specific properties related to job deployment activities.'
        }
        DRMMActivityLogDetailsDeviceJobGeneric = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLogDetailsDeviceJobGeneric class is used for DEVICE entity job category activity logs where the specific action is not yet mapped to a dedicated class. It inherits the 11 base properties common to all DEVICE job activities and dynamically adds any additional properties found in the response that are not part of the base class. This ensures type safety for known properties while maintaining flexibility for unknown actions.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a generic DEVICE job activity log details for unknown job actions, with base properties and dynamic additional properties.'
        }
        DRMMActivityLogDetailsDeviceRemote = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLogDetailsDeviceRemote class serves as a base class for DEVICE entity remote category activity logs. It encapsulates properties that are common across different remote session actions (chat, jrto, etc.), including remote session details, site information, user information, and source forwarding details, in addition to the entity-level DEVICE properties inherited from DRMMActivityLogEntityDevice. Specific remote action types inherit from this class and add their unique properties if needed.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                RemoteSessionDetails = 'An array of DRMMActivityLogDetailsRemoteSessionDetail objects describing individual events or steps within the remote session.'
                RemoteSessionId = 'The numeric identifier of the remote session.'
                RemoteSessionStartDate = 'The date and time when the remote session started.'
                RemoteSessionType = 'The type of remote session (e.g., chat, jrto).'
                SiteName = 'The name of the site associated with the remote session.'
                SourceForwardedIp = 'The forwarded IP address of the source that initiated the remote session.'
                UserEmail = 'The email address of the user who initiated the remote session.'
                UserFirstName = 'The first name of the user who initiated the remote session.'
                UserId = 'The identifier of the user who initiated the remote session.'
                UserLastName = 'The last name of the user who initiated the remote session.'
                UserUsername = 'The username of the user who initiated the remote session.'
            }
            RelatedLinks = @()
            ShortDescription = 'Base class for DEVICE remote-related activity log details, containing properties common to all remote session actions.'
        }
        DRMMActivityLogDetailsDeviceRemoteChat = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLogDetailsDeviceRemoteChat class models the details of a remote chat session activity log entry. It inherits common remote session properties from DRMMActivityLogDetailsDeviceRemote. Currently, chat actions share all base properties with no unique properties identified, but this class allows for future expansion if chat-specific properties are discovered.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
            }
            RelatedLinks = @()
            ShortDescription = 'Represents an activity log of entity DEVICE, category remote, and action chat, which includes specific properties related to remote chat session activities.'
        }
        DRMMActivityLogDetailsDeviceRemoteGeneric = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLogDetailsDeviceRemoteGeneric class is used for DEVICE entity remote category activity logs where the specific action is not yet mapped to a dedicated class. It inherits the 17 base properties common to all DEVICE remote activities and dynamically adds any additional properties found in the response that are not part of the base class. This ensures type safety for known properties while maintaining flexibility for unknown actions.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a generic DEVICE remote activity log details for unknown remote actions, with base properties and dynamic additional properties.'
        }
        DRMMActivityLogDetailsDeviceRemoteJrto = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLogDetailsDeviceRemoteJrto class models the details of a Jump Remote Take Over (jrto) activity log entry. It inherits common remote session properties from DRMMActivityLogDetailsDeviceRemote. Currently, jrto actions share all base properties with no unique properties identified, but this class allows for future expansion if jrto-specific properties are discovered.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
            }
            RelatedLinks = @()
            ShortDescription = 'Represents an activity log of entity DEVICE, category remote, and action jrto (Jump Remote Take Over), which includes specific properties related to JRTO session activities.'
        }
        DRMMActivityLogDetailsGeneric = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLogDetailsGeneric class is a flexible implementation of the DRMMActivityLogDetails class that can accommodate any structure of details returned by the API. It takes a PSCustomObject as input and dynamically adds its properties to the class instance. The class also includes logic to attempt parsing any properties that contain "date" in their name as date values, while retaining the original value if parsing fails. This allows it to handle a wide variety of detail structures without requiring predefined properties.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a generic implementation of the DRMMActivityLogDetails class, which can handle arbitrary key-value pairs from the API response.'
        }
        DRMMActivityLogDetailsRemoteSessionDetail = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLogDetailsRemoteSessionDetail class models individual detail items within the remote_session'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Action = 'The action type performed during this step of the remote session.'
                Detail = 'The detail text describing the specific event or step within the remote session.'
                Name = 'The name associated with this detail item in the remote session.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a detail item within a remote session activity log, including action, detail text, and name.'
        }
        DRMMActivityLogDetailsUserGeneric = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLogDetailsUserGeneric class is used for USER entity activity logs. As of this implementation, USER entity activities have not been observed in the wild, so this class serves as a placeholder and generic handler. It inherits base properties common to USER activities and dynamically adds any additional properties found in the response. This ensures graceful handling when USER activities are encountered.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a generic USER entity activity log for unknown categories, with entity-level properties and dynamic additional properties.'
        }
        DRMMActivityLogEntityDevice = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLogEntityDevice class serves as a base class for all DEVICE entity activity logs, regardless of category. It encapsulates the 6 core properties that appear in all DEVICE activities: DeviceHostname, DeviceUid, Entity, EventAction, EventCategory, and Uid. Category-specific classes (job, remote, device) inherit from this class and add their category-specific properties.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                DeviceHostname = 'The hostname of the device associated with the activity.'
                DeviceUid = 'The unique identifier (UID) of the device associated with the activity.'
                Entity = 'The entity type of the activity log entry (e.g., DEVICE).'
                EventAction = 'The specific action that was performed (e.g., deployment, create, move.device).'
                EventCategory = 'The category of the event (e.g., job, remote, device).'
                Uid = 'The unique identifier of the activity log detail entry.'
            }
            RelatedLinks = @()
            ShortDescription = 'Base class for DEVICE entity activity log details, containing properties common to all DEVICE activities.'
        }
        DRMMActivityLogEntityUser = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLogEntityUser class serves as a base class for all USER entity activity logs. As of this implementation, USER entity activities have not been observed in the wild, so this class is a placeholder for future expansion. It will likely contain properties such as UserId, UserUsername, Entity, EventAction, EventCategory, and Uid once USER activities are documented.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Entity = 'The entity type of the activity log entry (e.g., USER).'
                EventAction = 'The specific action that was performed in the user activity.'
                EventCategory = 'The category of the user event.'
                Uid = 'The unique identifier of the activity log detail entry.'
            }
            RelatedLinks = @()
            ShortDescription = 'Base class for USER entity activity log details, containing properties common to all USER activities.'
        }
        DRMMActivityLogSite = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLogSite class models the site information related to an activity log entry in the DRMM platform. It encapsulates properties such as the site ID and name. The class provides a static method to create an instance of the class from a typical API response object that contains these site details. This class is used as a property within the DRMMActivityLog class to provide additional context about the site associated with the activity log entry.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Id = 'The unique identifier for the site associated with the activity log entry.'
                Name = 'The name of the site associated with the activity log entry.'
            }
            RelatedLinks = @(
                'DRMMActivityLog'
                'Get-RMMActivityLog'
            )
            ShortDescription = 'Represents site information associated with a DRMM activity log entry, including site ID and name.'
        }
        DRMMActivityLogUser = @{
            Examples = @{
            }
            LongDescription = 'The DRMMActivityLogUser class models the user information related to an activity log entry in the DRMM platform. It encapsulates properties such as the user ID, username, first name, and last name. The class provides a static method to create an instance of the class from a typical API response object that contains these user details. Additionally, it includes a method to generate a summary string that combines the user''s first name, last name, and username for easy display in contexts where user information is relevant.'
            MethodDescriptions = @{
                GetSummary = @{
                    Description = 'Generates a summary string for the user, including their first name, last name, and username.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string combining the user''s first name, last name, and username.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                FirstName = 'The first name of the user associated with the activity log entry.'
                Id = 'The unique identifier for the user associated with the activity log entry.'
                LastName = 'The last name of the user associated with the activity log entry.'
                UserName = 'The username of the user associated with the activity log entry.'
            }
            RelatedLinks = @(
                'DRMMActivityLog'
                'Get-RMMActivityLog'
            )
            ShortDescription = 'Represents user information associated with a DRMM activity log entry, including user ID, username, and name details.'
        }
    }
    DRMMAlert = @{
        DRMMAlert = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlert class models an alert within the DRMM platform, encapsulating properties such as the alert''s unique identifier, priority, diagnostics, resolution status, ticket number, timestamp, and related information about the alert monitor, context, source, and response actions. It provides a static method to create an instance of the class from a typical API response object that contains alert information. The class also includes methods to determine if the alert is open or of certain priority levels, to resolve the alert, and to generate a summary string that combines key properties of the alert for easy display. The related classes DRMMAlertContext, DRMMAlertMonitorInfo, DRMMAlertSourceInfo, and DRMMAlertResponseAction represent nested information about the alert''s context, monitoring configuration, source details, and response actions taken, respectively.'
            MethodDescriptions = @{
                GetSummary = @{
                    Description = 'Gets a summary of the alert.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string combining key properties of the alert for easy display.'
                }
                IsCritical = @{
                    Description = 'Determines if the alert is of priority level "Critical".'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'True if the alert is of priority level "Critical", otherwise false.'
                }
                IsHigh = @{
                    Description = 'Determines if the alert is of priority level "High".'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'True if the alert is of priority level "High", otherwise false.'
                }
                IsOpen = @{
                    Description = 'Determines if the alert is currently open (not resolved).'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'True if the alert is currently open (not resolved), otherwise false.'
                }
                OpenPortal = @{
                    Description = 'Opens the alert''s portal URL in the default web browser.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'This method does not return a value. It performs an action to open the portal URL in the default web browser.'
                }
                Resolve = @{
                    Description = 'Resolves the alert.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'An updated instance of the DRMMAlert class with the alert marked as resolved.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                _Deprecated_SiteUid = 'Unique identifier for the site associated with the alert.'
                AlertContext = 'AlertContext of the DRMMAlert object, providing contextual information about the alert.'
                AlertMonitorInfo = 'AlertMonitorInfo of the DRMMAlert object, containing details about the alert monitor configuration.'
                AlertSourceInfo = 'AlertSourceInfo of the DRMMAlert object, including information about the source of the alert such as device and site details.'
                AlertUid = 'Unique identifier for the alert.'
                AutoresolveMins = 'The number of minutes after which the alert will be automatically resolved if not resolved manually.'
                Diagnostics = 'Diagnostic information related to the alert.'
                Muted = 'Indicates whether the alert is muted.'
                PortalUrl = 'The URL to access the alert in the Datto RMM web portal.'
                Priority = 'Priority level of the alert.'
                Resolved = 'Indicates whether the alert has been resolved.'
                ResolvedBy = 'Identifier of the user who resolved the alert.'
                ResolvedOn = 'Timestamp when the alert was resolved.'
                ResponseActions = 'Actions taken in response to the alert.'
                TicketNumber = 'Ticket number associated with the alert.'
                Timestamp = 'Timestamp when the alert was created.'
            }
            RelatedLinks = @(
                'DRMMAlertContext'
                'DRMMAlertMonitorInfo'
                'DRMMAlertSourceInfo'
                'DRMMAlertResponseAction'
                'Get-RMMAlert'
                'Resolve-RMMAlert'
            )
            ShortDescription = 'Represents an alert in the DRMM system, including its properties, context, source information, and response actions.'
        }
        DRMMAlertContext = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContext class models the context information associated with an alert in the DRMM platform. It includes a property for the class of the context, which indicates the type of alert context (e'
            MethodDescriptions = @{
                GetSummary = @{
                    Description = 'Gets a summary of the alert context.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string of the alert context.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                Class = 'The class of the alert context, indicating the type of context information associated with the alert.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContextAction'
                'DRMMAlertContextAntivirus'
                'DRMMAlertContextBackupManagement'
                'DRMMAlertContextCustomSNMP'
                'DRMMAlertContextDiskHealth'
                'DRMMAlertContextDiskUsage'
                'DRMMAlertContextEndpointSecurityThreat'
                'DRMMAlertContextEndpointSecurityWindowsDefender'
                'DRMMAlertContextEventLog'
                'DRMMAlertContextFan'
                'DRMMAlertContextFileSystem'
                'DRMMAlertContextGeneric'
                'DRMMAlertContextNetworkMonitor'
                'DRMMAlertContextOnlineOfflineStatus'
                'DRMMAlertContextPatch'
                'DRMMAlertContextPing'
                'DRMMAlertContextPrinter'
                'DRMMAlertContextPsu'
                'DRMMAlertContextRansomWare'
                'DRMMAlertContextResourceUsage'
                'DRMMAlertContextScript'
                'DRMMAlertContextSecCenter'
                'DRMMAlertContextSecurityManagement'
                'DRMMAlertContextSNMPProbe'
                'DRMMAlertContextStatus'
                'DRMMAlertContextTemperature'
                'DRMMAlertContextWindowsPerformance'
                'DRMMAlertContextWmi'
            )
            ShortDescription = 'Represents the context of an alert in the DRMM system, including its class and specific details based on the type of alert.'
        }
        DRMMAlertContextAction = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextAction class models the context information specific to software action alerts in the DRMM platform. It encapsulates properties such as the package name, action type (installed, uninstalled, or version changed), previous version, and current version that provide detailed context about the software action that triggered the alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                ActionType = 'The type of action that triggered the alert (e.g., INSTALLED, UNINSTALLED, VERSION_CHANGED).'
                PackageName = 'The name of the software package associated with the action alert.'
                PrevVersion = 'The previous version of the software package before the action.'
                Version = 'The current version of the software package after the action.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of an action alert in the DRMM system, including package name, action type, and version information.'
        }
        DRMMAlertContextAntivirus = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextAntivirus class models the context information specific to antivirus alerts in the DRMM platform. It encapsulates properties such as the antivirus status and product name that provide detailed context about the antivirus alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                ProductName = 'The name of the antivirus product associated with the alert context.'
                Status = 'The current status of the antivirus alert context.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of an antivirus alert in the DRMM system, including status and product name.'
        }
        DRMMAlertContextBackupManagement = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextBackupManagement class models the context information specific to backup management alerts in the DRMM platform. It encapsulates properties such as error messages and timeout values that are relevant to backup management scenarios.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                ErrorMessage = 'The error message associated with the backup management alert context.'
                Timeout = 'The timeout value related to the backup management alert context.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a backup management alert in the DRMM system, including error messages and timeout information.'
        }
        DRMMAlertContextCustomSNMP = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextCustomSNMP class models the context information specific to custom SNMP alerts in the DRMM platform. It encapsulates properties such as the display name of the alert, the current value that triggered the alert, and the monitor instance associated with the alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                CurrentValue = 'The current value that triggered the custom SNMP alert.'
                DisplayName = 'The display name of the custom SNMP alert.'
                MonitorInstance = 'The monitor instance associated with the custom SNMP alert.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a custom SNMP alert in the DRMM system, including display name, current value, and monitor instance information.'
        }
        DRMMAlertContextDiskHealth = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextDiskHealth class models the context information specific to disk health alerts in the DRMM platform. It encapsulates properties such as the reason for the alert and the type of disk health issue that was detected, providing insights into the underlying cause of the alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Reason = 'The reason for the disk health alert.'
                Type = 'The type of disk health issue detected.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a disk health alert in the DRMM system, including the reason for the alert and the type of issue detected.'
        }
        DRMMAlertContextDiskUsage = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextDiskUsage class models the context information specific to disk usage alerts in the DRMM platform. It encapsulates properties such as the name of the disk, total volume, free space, unit of measure, and disk name designation. This information provides insights into the disk usage conditions that triggered the alert, allowing for better understanding and response to disk-related issues.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                DiskName = 'The name of the disk associated with the disk usage alert.'
                DiskNameDesignation = 'The designation or label of the disk associated with the disk usage alert.'
                FreeSpace = 'The amount of free space available on the disk.'
                TotalVolume = 'The total volume or capacity of the disk.'
                UnitOfMeasure = 'The unit of measure used for disk space values (e.g., bytes, megabytes).'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a disk usage alert in the DRMM system, including details about the disk, total volume, free space, and unit of measure.'
        }
        DRMMAlertContextEndpointSecurityThreat = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextEndpointSecurityThreat class models the context information specific to endpoint security threat alerts in the DRMM platform. It encapsulates properties such as the alert ID and a description of the threat, providing detailed information about the security event that triggered the alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Description = 'A description of the endpoint security threat.'
                EsAlertId = 'The unique identifier for the endpoint security alert.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of an endpoint security threat alert in the DRMM system, including alert ID and description.'
        }
        DRMMAlertContextEndpointSecurityWindowsDefender = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextEndpointSecurityWindowsDefender class models the context information specific to endpoint security Windows Defender alerts in the DRMM platform. It encapsulates properties such as the alert ID and a description of the threat, providing detailed information about the security event that triggered the alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Description = 'A description of the endpoint security Windows Defender threat.'
                EsAlertId = 'The unique identifier for the endpoint security Windows Defender alert.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of an endpoint security Windows Defender alert in the DRMM system, including alert ID and description.'
        }
        DRMMAlertContextEventLog = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextEventLog class models the context information specific to event log alerts in the DRMM platform. It encapsulates properties such as the log name, code, type, source, description, trigger count, last triggered time, and whether the event caused a suspension. This information provides detailed insights into the event log conditions that triggered the alert, facilitating better understanding and response to event log-related issues.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                CausedSuspension = 'Indicates whether the event caused a suspension.'
                Code = 'The code associated with the event log alert.'
                Description = 'A description of the event log alert.'
                LastTriggered = 'The last time the event log alert was triggered.'
                LogName = 'The name of the event log.'
                Source = 'The source of the event log alert.'
                TriggerCount = 'The number of times the event log alert has been triggered.'
                Type = 'The type of the event log alert.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of an event log alert in the DRMM system, including log name, code, type, source, description, trigger count, last triggered time, and suspension status.'
        }
        DRMMAlertContextFan = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextFan class models the context information specific to fan alerts in the DRMM platform. It encapsulates properties such as the reason for the alert and the type of fan issue, providing detailed information about the hardware event that triggered the alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Reason = 'The reason for the fan alert.'
                Type = 'The type of fan issue detected.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a fan alert in the DRMM system, including reason and type.'
        }
        DRMMAlertContextFileSystem = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextFileSystem class models the context information specific to file system alerts in the DRMM platform. It encapsulates properties such as a sample value that triggered the alert, the threshold that was exceeded, the path of the file or directory involved, the type of object (file or directory), and the condition that caused the alert. This information provides detailed insights into the file system conditions that triggered the alert, facilitating better understanding and response to file system-related issues.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Condition = 'The condition that caused the file system alert.'
                ObjectType = 'The type of object involved in the alert (e.g., file or directory).'
                Path = 'The path of the file or directory involved in the alert.'
                Sample = 'A sample value that triggered the alert.'
                Threshold = 'The threshold that was exceeded to trigger the alert.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a file system alert in the DRMM system, including sample value, threshold, path, object type, and condition.'
        }
        DRMMAlertContextGeneric = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextGeneric class models a generic alert context in the DRMM platform. It is used when specific context class information is not available, encapsulating a hashtable of properties that provide detailed information about the alert context, along with a companion hashtable that records the'
            MethodDescriptions = @{
                GetSummary = @{
                    Description = 'Gets a summary of the generic alert context, including the class name and property names.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = $null
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                Properties = 'A hashtable containing properties that provide detailed information about the alert context when specific context class information is not available.'
                PropertyTypes = $null
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents a generic alert context in the DRMM system when specific context class information is not available.'
        }
        DRMMAlertContextNetworkMonitor = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextNetworkMonitor class models the context information specific to network monitor alerts in the DRMM platform. It encapsulates a description property that provides detailed information about the network event that triggered the alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Description = 'A description of the network monitor alert.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a network monitor alert in the DRMM system, including a description of the alert.'
        }
        DRMMAlertContextOnlineOfflineStatus = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextOnlineOfflineStatus class models the context information specific to online/offline status alerts in the DRMM platform. It encapsulates a status property that indicates the current online or offline state associated with the alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Status = 'The current online or offline status associated with the alert.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of an online/offline status alert in the DRMM system, including the current status.'
        }
        DRMMAlertContextPatch = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextPatch class models the context information specific to patch alerts in the DRMM platform. It encapsulates properties such as patch UID, policy UID, result, and additional information that provide detailed context about the patch alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Info = 'Additional information about the patch alert.'
                PatchUid = 'The unique identifier for the patch.'
                PolicyUid = 'The unique identifier for the policy associated with the patch.'
                Result = 'The result of the patch operation.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a patch alert in the DRMM system, including patch UID, policy UID, result, and additional information.'
        }
        DRMMAlertContextPing = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextPing class models the context information specific to ping alerts in the DRMM platform. It encapsulates properties such as instance name, roundtrip time, and reasons that provide detailed context about the ping alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                InstanceName = 'The name of the instance associated with the ping alert.'
                Reasons = 'The reasons for the ping alert.'
                RoundtripTime = 'The roundtrip time of the ping.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a ping alert in the DRMM system, including instance name, roundtrip time, and reasons for the alert.'
        }
        DRMMAlertContextPrinter = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextPrinter class models the context information specific to printer alerts in the DRMM platform. It encapsulates properties such as IP address, MAC address, marker supply index, and current level that provide detailed context about the printer alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                CurrentLevel = 'The current level of the printer marker supply.'
                IpAddress = 'The IP address of the printer.'
                MacAddress = 'The MAC address of the printer.'
                MarkerSupplyIndex = 'The index of the marker supply in the printer.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a printer alert in the DRMM system, including IP address, MAC address, marker supply index, and current level.'
        }
        DRMMAlertContextPsu = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextPsu class models the context information specific to PSU alerts in the DRMM platform. It encapsulates properties such as reason and type that provide detailed context about the PSU alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Reason = 'The reason for the PSU alert.'
                Type = 'The type of PSU alert.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a PSU (Power Supply Unit) alert in the DRMM system, including reason and type of the alert.'
        }
        DRMMAlertContextRansomWare = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextRansomWare class models the context information specific to ransomware alerts in the DRMM platform. It encapsulates properties such as state, confidence factor, affected directories, watch paths, ransomware extension, and alert times that provide detailed context about the ransomware alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                AffectedDirectories = 'The directories affected by the ransomware.'
                AlertTime = 'The time when the ransomware alert was generated.'
                ConfidenceFactor = 'The confidence factor indicating the likelihood of a ransomware event.'
                MetaAlertTime = 'The time when the meta alert related to the ransomware was generated.'
                Rwextension = 'The ransomware extension associated with the alert.'
                State = 'The current state of the ransomware alert.'
                WatchPaths = 'The paths being watched for ransomware activity.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a ransomware alert in the DRMM system, including state, confidence factor, affected directories, watch paths, ransomware extension, and alert times.'
        }
        DRMMAlertContextResourceUsage = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextResourceUsage class models the context information specific to resource usage alerts in the DRMM platform. It encapsulates properties such as process name, sample value, and type that provide detailed context about the resource usage alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                ProcessName = 'The name of the process associated with the resource usage alert.'
                Sample = 'The sample value indicating the resource usage.'
                Type = 'The type of resource being monitored.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a resource usage alert in the DRMM system, including process name, sample value, and type of resource.'
        }
        DRMMAlertContextScript = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextScript class models the context information specific to script alerts in the DRMM platform. It encapsulates properties such as a hashtable of sample values that provide detailed context about the script alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Samples = 'A hashtable of sample values associated with the script alert.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a script alert in the DRMM system, including a hashtable of sample values.'
        }
        DRMMAlertContextSecCenter = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextSecCenter class models the context information specific to security center alerts in the DRMM platform. It encapsulates properties such as product name and alert type that provide detailed context about the security center alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                AlertType = 'The type of the security center alert.'
                ProductName = 'The name of the product generating the security center alert.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a security center alert in the DRMM system, including product name and alert type.'
        }
        DRMMAlertContextSecurityManagement = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextSecurityManagement class models the context information specific to security management alerts in the DRMM platform. It encapsulates properties such as status, product name, information time, virus name, infected files, and other related properties that provide detailed context about the security management alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                ExpiryLicenseForDays = 'The number of days until the license expires.'
                InfectedFiles = 'The files infected by the security threat.'
                InfoTime = 'The time when the information was recorded.'
                ProductName = 'The name of the product generating the security management alert.'
                ProductNotUpdatedForDays = 'The number of days the product has not been updated.'
                Status = 'The current status of the security management alert.'
                SystemRemainsInfectedForHours = 'The number of hours the system remains infected.'
                VirusName = 'The name of the virus associated with the alert.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a security management alert in the DRMM system, including status, product name, information time, virus name, infected files, and other related properties.'
        }
        DRMMAlertContextSNMPProbe = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextSNMPProbe class models the context information specific to SNMP probe alerts in the DRMM platform. It encapsulates properties such as IP address, OID, rule name, response value, device name, and monitor name that provide detailed context about the SNMP probe alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                DeviceName = 'The name of the device associated with the SNMP probe alert.'
                IpAddress = 'The IP address involved in the SNMP probe alert.'
                MonitorName = 'The name of the monitor related to the SNMP probe alert.'
                Oid = 'The Object Identifier (OID) relevant to the SNMP probe alert.'
                ResponseValue = 'The response value received from the SNMP probe.'
                RuleName = 'The name of the rule that triggered the SNMP probe alert.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of an SNMP probe alert in the DRMM system, including IP address, OID, rule name, response value, device name, and monitor name.'
        }
        DRMMAlertContextStatus = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextStatus class models the context information specific to status alerts in the DRMM platform. It encapsulates properties such as process name and status that provide detailed context about the status alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                ProcessName = 'The name of the process associated with the status alert.'
                Status = 'The current status of the process.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a status alert in the DRMM system, including process name and status information.'
        }
        DRMMAlertContextTemperature = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextTemperature class models the context information specific to temperature alerts in the DRMM platform. It encapsulates properties such as degree and type that provide detailed context about the temperature alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Degree = 'The degree of the temperature alert.'
                Type = 'The type of temperature alert.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a temperature alert in the DRMM system, including degree and type of temperature issue.'
        }
        DRMMAlertContextWindowsPerformance = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextWindowsPerformance class models the context information specific to Windows performance alerts in the DRMM platform. It encapsulates properties such as value that provide detailed context about the Windows performance alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Value = 'The value that indicates the Windows performance metric.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a Windows performance alert in the DRMM system, including a value that indicates the performance metric.'
        }
        DRMMAlertContextWmi = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertContextWmi class models the context information specific to WMI alerts in the DRMM platform. It encapsulates properties such as value that provide detailed context about the WMI alert.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Value = 'The value that indicates the WMI metric or status.'
            }
            RelatedLinks = @(
                'DRMMAlert'
                'DRMMAlertContext'
            )
            ShortDescription = 'Represents the context of a WMI alert in the DRMM system, including a value that indicates the WMI metric or status.'
        }
        DRMMAlertMonitorInfo = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertMonitorInfo class models the monitor information specific to alerts in the DRMM platform. It encapsulates properties such as SendsEmails and CreatesTicket that provide detailed context about the alert''s monitoring configuration.'
            MethodDescriptions = @{
                GetSummary = @{
                    Description = 'Generates a summary string for the alert monitor information, indicating whether it sends emails and creates tickets.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string indicating the email and ticket creation configuration of the alert monitor.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                CreatesTicket = 'Indicates whether the alert creates a ticket.'
                SendsEmails = 'Indicates whether the alert sends emails.'
            }
            RelatedLinks = @(
                'DRMMAlert'
            )
            ShortDescription = 'Represents the monitor information for an alert in the DRMM system, including whether the alert sends emails and creates tickets.'
        }
        DRMMAlertResponseAction = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertResponseAction class models the response actions specific to alerts in the DRMM platform. It encapsulates properties such as ActionTime, ActionType, Description, ActionReference, and ActionReferenceInt that provide detailed context about the response actions taken for an alert.'
            MethodDescriptions = @{
                GetSummary = @{
                    Description = 'Generates a summary string for the alert response action, including action type and description.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string for the alert response action, including action type and description.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                ActionReference = 'Reference to the action taken.'
                ActionReferenceInt = 'Internal reference identifier for the action.'
                ActionTime = 'The time when the action was taken.'
                ActionType = 'The type of action taken.'
                Description = 'Description of the action taken.'
            }
            RelatedLinks = @(
                'DRMMAlert'
            )
            ShortDescription = 'Represents a response action taken for an alert in the DRMM system, including action time, type, description, and references.'
        }
        DRMMAlertSourceInfo = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAlertSourceInfo class models the source information specific to alerts in the DRMM platform. It encapsulates properties such as DeviceUid, DeviceName, SiteUid, and SiteName that provide detailed context about the alert''s source.'
            MethodDescriptions = @{
                GetSummary = @{
                    Description = 'Generates a summary string for the alert source information, including device and site details.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string for the alert source information, including device and site details.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                DeviceName = 'The name of the device associated with the alert.'
                DeviceUid = 'The unique identifier of the device associated with the alert.'
                SiteName = 'The name of the site associated with the alert.'
                SiteUid = 'The unique identifier of the site associated with the alert.'
            }
            RelatedLinks = @(
                'DRMMAlert'
            )
            ShortDescription = 'Represents the source information for an alert in the DRMM system, including device and site details.'
        }
    }
    DRMMAPIKeySecret = @{
        DRMMAPIKeySecret = @{
            Examples = @{
            }
            LongDescription = 'The DRMMAPIKeySecret class encapsulates the API key, API secret, and associated username for a DRMM account. It provides a static method to create an instance of the class from a typical API response object that contains these credentials. The API secret is stored as a secure string to enhance security when handling sensitive information.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                ApiKey = 'API authentication key.'
                ApiSecret = 'API authentication secret.'
                Username = 'Username associated with the API key and secret.'
            }
            RelatedLinks = @(
                'Reset-RMMAPIKeySecret'
            )
            ShortDescription = 'Represents API key and secret information for authenticating with the DRMM API.'
        }
    }
    DRMMComponent = @{
        DRMMComponent = @{
            Examples = @{
            }
            LongDescription = 'The DRMMComponent class models a component within the DRMM platform, encapsulating properties such as Id, Uid, Name, Description, CategoryCode, CredentialsRequired, and an array of associated variables (DRMMComponentVariable). It provides methods to retrieve specific variables and generate summaries of the component''s properties.'
            MethodDescriptions = @{
                GetInputVariables = @{
                    Description = 'Retrieves all input variables associated with the component.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'An array of DRMMComponentVariable objects that are designated as input variables for the component.'
                }
                GetOutputVariables = @{
                    Description = 'Retrieves all output variables associated with the component.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'An array of DRMMComponentVariable objects that are designated as output variables for the component.'
                }
                GetSummary = @{
                    Description = 'Generates a summary string for the component, including its name, variable count, credentials requirement, and category.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string for the component, including its name, variable count, credentials requirement, and category.'
                }
                GetVariable = @{
                    Description = 'Retrieves a specific variable from the component by name.'
                    Example = $null
                    Parameters = @{
                        Name = 'The name of the variable to retrieve.'
                    }
                    ReturnValue = 'The DRMMComponentVariable object that matches the specified name, or null if not found.'
                }
                OpenPortal = @{
                    Description = 'Opens the component''s portal URL in the default web browser.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'This method does not return a value. It performs an action to open the portal URL in the default web browser.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                CategoryCode = 'The category code that classifies the component within the DRMM system.'
                CredentialsRequired = 'Indicates whether the component requires credentials.'
                Description = 'A description of the component.'
                Id = 'The unique identifier of the component.'
                Name = 'The name of the component.'
                PortalUrl = 'The URL to access the component in the Datto RMM web portal.'
                Uid = 'The unique identifier string of the component.'
                Variables = 'An array of variables associated with the component.'
            }
            RelatedLinks = @(
                'Get-RMMComponent'
                'New-RMMQuickJob'
                'DRMMComponentVariable'
            )
            ShortDescription = 'Represents a component in the DRMM system, including its properties and associated variables.'
        }
        DRMMComponentVariable = @{
            Examples = @{
            }
            LongDescription = 'The DRMMComponentVariable class models a variable that can be used as input or output for a DRMM component. It includes properties for the variable''s name, default value, type, direction (input/output), description, and index within the component''s variable list. Methods allow for instantiation from API responses and for generating a summary string describing the variable.'
            MethodDescriptions = @{
                GetSummary = @{
                    Description = 'Generates a summary string for the component variable.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string for the component variable.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                DefaultValue = 'The default value of the variable.'
                Description = 'A description of the variable.'
                Direction = 'The direction of the variable (input or output).'
                Index = 'The index of the variable within the component''s variable list.'
                Name = 'The name of the variable.'
                Type = 'The data type of the variable.'
            }
            RelatedLinks = @(
                'DRMMComponent'
            )
            ShortDescription = 'Represents a variable associated with a DRMM component, including its name, type, direction, and other metadata.'
        }
    }
    DRMMDevice = @{
        DRMMDevice = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDevice class models a device within the DRMM platform, providing properties that describe the device''s attributes and state, as well as methods to retrieve related information such as alerts and to perform actions like opening the device portal.'
            MethodDescriptions = @{
                ClearUDF = @{
                    Description = 'Clears the value of a specified User-Defined Field (UDF) for the device.'
                    Example = $null
                    Parameters = @{
                        UdfNumber = 'The number of the User-Defined Field (UDF) to clear.'
                    }
                    ReturnValue = 'This method does not return a value. It performs an action to clear the specified UDF.'
                }
                ClearUDFs = @{
                    Description = 'Clears the values of all User-Defined Fields (UDFs) for the device.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'This method does not return a value. It performs an action to clear all UDFs.'
                }
                GetAlerts = @{
                    Description = 'Retrieves the alerts associated with the device, filtered by a specified status.'
                    Example = $null
                    Parameters = @{
                        Status = 'The status to filter alerts by.'
                    }
                    ReturnValue = 'An array of alerts associated with the device that match the specified status.'
                }
                GetAudit = @{
                    Description = 'Gets the most recent audit information for this device.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'The most recent audit information for this device.'
                }
                GetSoftware = @{
                    Description = 'Gets the software information for this device.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'The software information for this device.'
                }
                GetSummary = @{
                    Description = 'Generates a summary string for the device, including its hostname and device type.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string for the device, including its hostname and device type.'
                }
                GetUdfAsCsv = @{
                    Description = 'Retrieves the value of a specified User-Defined Field (UDF) as a CSV object with a custom delimiter and headers.'
                    Example = $null
                    Parameters = @{
                        Delimiter = 'The delimiter to use in the CSV output (e.g., comma, semicolon).'
                        Headers = 'An array of headers to include in the CSV output.'
                        UdfNumber = 'The number of the User-Defined Field (UDF) to retrieve as CSV.'
                    }
                    ReturnValue = 'A CSV object containing the value of the specified UDF, formatted with the provided delimiter and headers.'
                }
                GetUdfAsJson = @{
                    Description = 'Retrieves the value of a specified User-Defined Field (UDF) as a JSON object.'
                    Example = $null
                    Parameters = @{
                        UdfNumber = 'The number of the User-Defined Field (UDF) to retrieve as JSON.'
                    }
                    ReturnValue = 'A JSON object containing the value of the specified UDF.'
                }
                Move = @{
                    Description = 'Moves the device to a different site within the DRMM system.'
                    Example = $null
                    Parameters = @{
                        TargetSiteUid = 'The unique identifier of the target site to move the device to.'
                    }
                    ReturnValue = 'This method does not return a value. It performs an action to move the device to the specified site.'
                }
                OpenPortal = @{
                    Description = 'Opens the device''s portal URL in the default web browser.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'This method does not return a value. It performs an action to open the portal URL in the default web browser.'
                }
                OpenWebRemote = @{
                    Description = 'Opens the device''s web remote URL in the default web browser.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'This method does not return a value. It performs an action to open the web remote URL in the default web browser.'
                }
                ResolveAllAlerts = @{
                    Description = 'Resolves all open alerts associated with the device.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'This method does not return a value. It performs an action to resolve all open alerts associated with the device.'
                }
                RunQuickJob = @{
                    Description = 'Runs a quick job on the device for a specified job component and variables.'
                    Example = $null
                    Parameters = @{
                        ComponentUid = 'The unique identifier of the job component to run.'
                        Variables = 'A hashtable of variables to pass to the job component.'
                    }
                    ReturnValue = 'A DRMMJob object representing the job that was run on the device.'
                }
                SetUDF = @{
                    Description = 'Sets the value of one or more User-Defined Fields (UDFs) for the device.'
                    Example = $null
                    Parameters = @{
                        UDFFields = 'A hashtable of User-Defined Fields (UDFs) to set for the device.'
                    }
                    ReturnValue = 'This method does not return a value. It performs an action to set the specified UDFs for the device.'
                }
                SetWarranty = @{
                    Description = 'Sets the warranty date for the device.'
                    Example = $null
                    Parameters = @{
                        WarrantyDate = 'The warranty date to set for the device.'
                    }
                    ReturnValue = 'This method does not return a value. It performs an action to set the warranty date for the device.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                A64Bit = 'Indicates whether the device is running a 64-bit operating system.'
                Antivirus = 'Information about the device''s antivirus software.'
                CagVersion = 'The version of the CAG agent installed on the device.'
                CreationDate = 'The date when the device was created in the DRMM system.'
                Deleted = 'Indicates whether the device has been marked as deleted.'
                Description = 'The device''s description.'
                DeviceClass = 'The class of the device, which may indicate its role or type within the organization.'
                DeviceType = 'The type of the device.'
                DisplayVersion = 'The display version of the device.'
                Domain = 'The domain to which the device belongs.'
                ExtIpAddress = 'The external IP address of the device.'
                Hostname = 'The hostname of the device.'
                Id = 'The unique identifier of the device.'
                IntIpAddress = 'The internal IP address of the device.'
                LastAuditDate = 'The date when the device was last audited.'
                LastLoggedInUser = 'The user who last logged into the device.'
                LastReboot = 'The date and time when the device was last rebooted.'
                LastSeen = 'The last time the device was seen online.'
                NetworkProbe = 'Information about the device''s network probe status.'
                OnboardedViaNetworkMonitor = 'Indicates whether the device was onboarded via network monitoring.'
                Online = 'Indicates whether the device is currently online.'
                OperatingSystem = 'The operating system running on the device.'
                PatchManagement = 'Information about the device''s patch management status.'
                PortalUrl = 'The URL to access the device''s portal in the DRMM system.'
                RebootRequired = 'Indicates whether the device requires a reboot.'
                RevealLastLoggedInUser = 'Indicates whether the last logged in user information is revealed for the device.'
                SiteId = 'The unique identifier of the site to which the device belongs.'
                SiteName = 'The name of the site to which the device belongs.'
                SiteUid = 'The unique identifier (UID) of the site to which the device belongs.'
                SnmpEnabled = 'Indicates whether SNMP is enabled on the device.'
                SoftwareStatus = 'Information about the device''s software status.'
                Suspended = 'Indicates whether the device is currently suspended in the DRMM system.'
                Udfs = 'User-defined fields associated with the device.'
                Uid = 'The unique identifier (UID) of the device.'
                WarrantyDate = 'The date when the device''s warranty expires.'
                WebRemoteUrl = 'The URL for web remote access to the device.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a device in the DRMM system, encapsulating properties and methods for interacting with the device.'
        }
        DRMMDeviceAntivirusInfo = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDeviceAntivirusInfo class models the antivirus information associated with a device in the DRMM platform. It includes properties such as AntivirusProduct and AntivirusStatus, which provide details about the antivirus software installed on the device and its current status. The class also includes methods to determine if the antivirus is running and up to date, as well as a method to generate a summary string of the antivirus information.'
            MethodDescriptions = @{
                GetSummary = @{
                    Description = 'Generates a summary string of the antivirus product and its status.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string of the antivirus product and its status.'
                }
                IsRunning = @{
                    Description = 'Determines if the antivirus is currently running on the device.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A boolean value indicating whether the antivirus is currently running on the device.'
                }
                IsUpToDate = @{
                    Description = 'Determines if the antivirus is running and up to date on the device.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A boolean value indicating whether the antivirus is running and up to date on the device.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                AntivirusProduct = 'The name of the antivirus product installed on the device.'
                AntivirusStatus = 'The current status of the antivirus product on the device.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents antivirus information for a device in the DRMM system, including the antivirus product name and its status.'
        }
        DRMMDeviceNetworkInterface = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDeviceNetworkInterface class models the network interface information for a device in the DRMM platform. It includes properties such as Id, Uid, SiteId, SiteUid, SiteName, DeviceType, Hostname, IntIpAddress, ExtIpAddress, and an array of network interfaces (Nics). The class provides a constructor and a static method to create an instance from API response data.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                DeviceType = 'The type of the network device.'
                ExtIpAddress = 'The external IP address of the network interface.'
                Hostname = 'The hostname associated with the network interface.'
                Id = 'The unique identifier of the network interface.'
                IntIpAddress = 'The internal IP address of the network interface.'
                Nics = 'The network interface cards associated with the device.'
                SiteId = 'The unique identifier of the site to which the network interface belongs.'
                SiteName = 'The name of the site to which the network interface belongs.'
                SiteUid = 'The unique identifier (UID) of the site to which the network interface belongs.'
                Uid = 'The unique identifier (UID) of the network interface.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a network interface associated with a device in the DRMM system.'
        }
        DRMMDevicePatchManagement = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDevicePatchManagement class models the patch management status for a device in the DRMM platform. It includes properties such as PatchStatus, PatchesApprovedPending, PatchesNotApproved, and PatchesInstalled, which provide insights into the device''s patch management state. The class provides a constructor and a static method to create an instance from API response data.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                PatchesApprovedPending = 'The number of patches that are approved but pending installation.'
                PatchesInstalled = 'The number of patches that have been installed on the device.'
                PatchesNotApproved = 'The number of patches that are not approved for installation.'
                PatchStatus = 'The current status of patch management on the device.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents patch management information for a device in the DRMM system.'
        }
        DRMMDeviceType = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDeviceType class models the type information for a device in the DRMM platform. It includes properties such as Category and Type. The class provides a constructor and a static method to create an instance from API response data.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Category = 'The category of the device type.'
                Type = 'The specific type of the device.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a device type in the DRMM system.'
        }
        DRMMDeviceUdfs = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDeviceUdfs class models the user-defined fields (UDFs) for a device in the DRMM platform. It includes properties for Udf1 through Udf30, which can store custom data defined by the user. The class provides a constructor and a static method to create an instance from API response data, populating the UDF properties based on the response.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Udf1 = 'The value of user-defined field 1 for the device.'
                Udf10 = 'The value of user-defined field 10 for the device.'
                Udf11 = 'The value of user-defined field 11 for the device.'
                Udf12 = 'The value of user-defined field 12 for the device.'
                Udf13 = 'The value of user-defined field 13 for the device.'
                Udf14 = 'The value of user-defined field 14 for the device.'
                Udf15 = 'The value of user-defined field 15 for the device.'
                Udf16 = 'The value of user-defined field 16 for the device.'
                Udf17 = 'The value of user-defined field 17 for the device.'
                Udf18 = 'The value of user-defined field 18 for the device.'
                Udf19 = 'The value of user-defined field 19 for the device.'
                Udf2 = 'The value of user-defined field 2 for the device.'
                Udf20 = 'The value of user-defined field 20 for the device.'
                Udf21 = 'The value of user-defined field 21 for the device.'
                Udf22 = 'The value of user-defined field 22 for the device.'
                Udf23 = 'The value of user-defined field 23 for the device.'
                Udf24 = 'The value of user-defined field 24 for the device.'
                Udf25 = 'The value of user-defined field 25 for the device.'
                Udf26 = 'The value of user-defined field 26 for the device.'
                Udf27 = 'The value of user-defined field 27 for the device.'
                Udf28 = 'The value of user-defined field 28 for the device.'
                Udf29 = 'The value of user-defined field 29 for the device.'
                Udf3 = 'The value of user-defined field 3 for the device.'
                Udf30 = 'The value of user-defined field 30 for the device.'
                Udf4 = 'The value of user-defined field 4 for the device.'
                Udf5 = 'The value of user-defined field 5 for the device.'
                Udf6 = 'The value of user-defined field 6 for the device.'
                Udf7 = 'The value of user-defined field 7 for the device.'
                Udf8 = 'The value of user-defined field 8 for the device.'
                Udf9 = 'The value of user-defined field 9 for the device.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents user-defined fields (UDFs) associated with a device in the DRMM system.'
        }
    }
    DRMMDeviceAudit = @{
        DRMMDeviceAudit = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDeviceAudit class encapsulates detailed information about a device, such as its unique identifier, portal URL, system information, network interfaces, BIOS details, baseboard information, display configurations, logical disks, mobile information, processors, video boards, attached devices, SNMP information, physical memory, and installed software. This class is typically used to represent the results of a device audit operation within the DRMM system.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                AttachedDevices = 'Information about devices attached to the audited device.'
                BaseBoard = 'Information about the baseboard (motherboard) of the audited device.'
                Bios = 'Information about the BIOS of the audited device.'
                DeviceUid = 'The unique identifier of the audited device.'
                Displays = 'Information about the display configurations of the audited device.'
                LogicalDisks = 'Information about the logical disks of the audited device.'
                MobileInfo = 'Information about the mobile aspects of the audited device.'
                Nics = 'Information about the network interfaces of the audited device.'
                PhysicalMemory = 'Information about the physical memory of the audited device.'
                PortalUrl = 'The portal URL associated with the audited device.'
                Processors = 'Information about the processors of the audited device.'
                SnmpInfo = 'Information about the SNMP configuration of the audited device.'
                Software = 'Information about the software installed on the audited device.'
                SystemInfo = 'Information about the system of the audited device.'
                VideoBoards = 'Information about the video boards of the audited device.'
                WebRemoteUrl = 'The web remote URL associated with the audited device.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a comprehensive audit of a device, including hardware, software, and network information.'
        }
        DRMMDeviceAuditAttachedDevice = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDeviceAuditAttachedDevice class models the information about a device that is attached to the audited system. It includes properties such as Description and Instance, which provide details about the attached device. This class is typically used as part of the DRMMDeviceAudit to represent the various devices connected to the system being audited.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Description = 'A description of the attached device.'
                Instance = 'The instance identifier of the attached device.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents an attached device in a device audit, including its description and instance information.'
        }
        DRMMDeviceAuditBaseBoard = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDeviceAuditBaseBoard class models the information about the baseboard (motherboard) of the audited system. It includes properties such as Manufacturer, Product, and SerialNumber, which provide details about the baseboard. This class is typically used as part of the DRMMDeviceAudit to represent the hardware information of the system being audited.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Manufacturer = 'The manufacturer of the baseboard.'
                Product = 'The product name or model of the baseboard.'
                SerialNumber = 'The serial number of the baseboard.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the baseboard information of a device in a device audit, including manufacturer, product, and serial number.'
        }
        DRMMDeviceAuditBios = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDeviceAuditBios class models the information about the BIOS of the audited system. It includes properties such as Manufacturer, Name, SerialNumber, and SmbiosBiosVersion, which provide details about the BIOS. This class is typically used as part of the DRMMDeviceAudit to represent the hardware information of the system being audited.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Manufacturer = 'The manufacturer of the BIOS.'
                Name = 'The name of the BIOS.'
                SerialNumber = 'The serial number of the BIOS.'
                SmbiosBiosVersion = 'The SMBIOS BIOS version.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the BIOS information of a device in a device audit, including manufacturer, name, serial number, and SMBIOS BIOS version.'
        }
        DRMMDeviceAuditDisplay = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDeviceAuditDisplay class models the information about the display of the audited system. It includes properties such as Instance, ScreenHeight, and ScreenWidth, which provide details about the display. This class is typically used as part of the DRMMDeviceAudit to represent the hardware information of the system being audited.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Instance = 'The instance identifier of the display.'
                ScreenHeight = 'The height of the display screen in pixels.'
                ScreenWidth = 'The width of the display screen in pixels.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the display information of a device in a device audit, including instance, screen height, and screen width.'
        }
        DRMMDeviceAuditLogicalDisk = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDeviceAuditLogicalDisk class models the information about the logical disks of the audited system. It includes properties such as Description, DiskIdentifier, Freespace, and Size, which provide details about each logical disk. This class is typically used as part of the DRMMDeviceAudit to represent the hardware information of the system being audited.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Description = 'A description of the logical disk.'
                DiskIdentifier = 'The identifier of the logical disk.'
                Freespace = 'The free space available on the logical disk.'
                Size = 'The total size of the logical disk.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the logical disk information of a device in a device audit, including description, disk identifier, free space, and size.'
        }
        DRMMDeviceAuditMobileInfo = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDeviceAuditMobileInfo class models the information about the mobile connectivity of the audited system. It includes properties such as Iccid, Imei, Number, and Operator, which provide details about the mobile network information of the device. This class is typically used as part of the DRMMDeviceAudit to represent the hardware information of the system being audited.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Iccid = 'The ICCID (Integrated Circuit Card Identifier) of the mobile device.'
                Imei = 'The IMEI (International Mobile Equipment Identity) of the mobile device.'
                Number = 'The phone number associated with the mobile device.'
                Operator = 'The mobile network operator of the device.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the mobile information of a device in a device audit, including ICCID, IMEI, number, and operator.'
        }
        DRMMDeviceAuditPhysicalMemory = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDeviceAuditPhysicalMemory class models the information about the physical memory modules of the audited system. It includes properties such as BankLabel, Capacity, Manufacturer, PartNumber, SerialNumber, and Speed, which provide details about each physical memory module. This class is typically used as part of the DRMMDeviceAudit to represent the hardware information of the system being audited.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                BankLabel = 'The label of the memory bank.'
                Capacity = 'The capacity of the physical memory module.'
                Manufacturer = 'The manufacturer of the physical memory module.'
                PartNumber = 'The part number of the physical memory module.'
                SerialNumber = 'The serial number of the physical memory module.'
                Speed = 'The speed of the physical memory module.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the physical memory information of a device in a device audit, including bank label, capacity, manufacturer, part number, serial number, and speed.'
        }
        DRMMDeviceAuditProcessor = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDeviceAuditProcessor class models the information about the processor(s) of the audited system. It includes a property for the Name of the processor, which provides details about the CPU. This class is typically used as part of the DRMMDeviceAudit to represent the hardware information of the system being audited.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Name = 'The name of the processor.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the processor information of a device in a device audit, including its name.'
        }
        DRMMDeviceAuditSnmpInfo = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDeviceAuditSnmpInfo class models the information about the SNMP configuration of the audited system. It includes properties such as Contact, Description, Location, and Name, which provide details about the SNMP settings of the device. This class is typically used as part of the DRMMDeviceAudit to represent the network information of the system being audited.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Contact = 'The contact information for SNMP.'
                Description = 'A description of the SNMP configuration.'
                Location = 'The physical location of the SNMP device.'
                Name = 'The name of the SNMP device.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the SNMP information of a device in a device audit, including contact, description, location, and name.'
        }
        DRMMDeviceAuditSoftware = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDeviceAuditSoftware class models the information about the software installed on the audited system. It includes properties such as Name and Version, which provide details about each software application. This class is typically used as part of the DRMMDeviceAudit to represent the software inventory of the system being audited.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Name = 'The name of the software.'
                Version = 'The version of the software.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the software information of a device in a device audit, including its name and version.'
        }
        DRMMDeviceAuditSystemInfo = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDeviceAuditSystemInfo class models the information about the system of the audited device. It includes properties such as Manufacturer, Model, TotalPhysicalMemory, Username, DotNetVersion, and TotalCpuCores, which provide detailed information about the system''s hardware and software environment. This class is typically used as part of the DRMMDeviceAudit to represent the overall system information of the device being audited.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                DotNetVersion = 'The version of the .NET framework installed on the system.'
                Manufacturer = 'The manufacturer of the system.'
                Model = 'The model of the system.'
                TotalCpuCores = 'The total number of CPU cores in the system.'
                TotalPhysicalMemory = 'The total physical memory (RAM) of the system.'
                Username = 'The username of the currently logged-in user.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the system information of a device in a device audit, including manufacturer, model, total physical memory, username,'
        }
        DRMMDeviceAuditVideoBoard = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDeviceAuditVideoBoard class models the information about the video board (graphics card) of the audited system. It includes a property for the DisplayAdapter, which provides details about the graphics hardware. This class is typically used as part of the DRMMDeviceAudit to represent the hardware information of the system being audited.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                DisplayAdapter = 'The name of the display adapter (video board) in the system.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the video board information of a device in a device audit, including its display adapter name.'
        }
    }
    DRMMEsxiHostAudit = @{
        DRMMEsxiDatastore = @{
            Examples = @{
            }
            LongDescription = 'The DRMMEsxiHostAudit class encapsulates detailed information about an ESXi host, such as its unique identifier, portal URL, system information, guest virtual machines, processors, network interfaces, physical memory modules, and datastores. This class is typically used to represent the results of an ESXi host audit operation within the DRMM system.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                DatastoreName = 'The name of the datastore.'
                FileSystem = 'The file system type of the datastore.'
                FreeSpace = 'The amount of free space available in the datastore.'
                Size = 'The total size of the datastore.'
                Status = 'The current status of the datastore.'
                SubscriptionPercent = 'The percentage of subscription used in the datastore.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the audit information of an ESXi host, including system info, guests, processors, network interfaces, physical memory, and datastores.'
        }
        DRMMEsxiGuest = @{
            Examples = @{
            }
            LongDescription = 'The DRMMEsxiGuest class models the information about a guest virtual machine running on an ESXi host. It includes properties such as GuestName, ProcessorSpeedTotal, MemorySizeTotal, NumberOfSnapshots, and Datastores, which provide details about the virtual machine''s configuration and resource usage. This class is typically used as part of the DRMMEsxiHostAudit to represent the virtual machines running on the ESXi host being audited.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Datastores = 'The datastores associated with the guest virtual machine.'
                GuestName = 'The name of the guest virtual machine.'
                MemorySizeTotal = 'The total memory size allocated to the guest virtual machine.'
                NumberOfSnapshots = 'The number of snapshots taken for the guest virtual machine.'
                ProcessorSpeedTotal = 'The total processor speed allocated to the guest virtual machine.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a guest virtual machine on an ESXi host, including its name, processor speed, memory size, number of snapshots, and datastores.'
        }
        DRMMEsxiHostAudit = @{
            Examples = @{
            }
            LongDescription = 'The DRMMEsxiHostAudit class encapsulates detailed information about an ESXi host, such as its unique identifier, portal URL, system information, guest virtual machines, processors, network interfaces, physical memory modules, and datastores. This class is typically used to represent the results of an ESXi host audit operation within the DRMM system.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Datastores = 'The datastores associated with the ESXi host.'
                DeviceUid = 'The unique identifier of the ESXi host.'
                Guests = 'The guest virtual machines running on the ESXi host.'
                Nics = 'The network interface cards (NICs) of the ESXi host.'
                PhysicalMemory = 'The physical memory modules of the ESXi host.'
                PortalUrl = 'The portal URL of the ESXi host.'
                Processors = 'The processors of the ESXi host.'
                SystemInfo = 'The system information of the ESXi host.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the audit information of an ESXi host, including system info, guests, processors, network interfaces, physical memory, and datastores.'
        }
        DRMMEsxiNic = @{
            Examples = @{
            }
            LongDescription = 'The DRMMEsxiNic class models the information about a network interface card (NIC) on an ESXi host. It includes properties such as Name, Ipv4, Ipv6, MacAddress, Speed, and Type, which provide details about the NIC''s configuration and capabilities. This class is typically used as part of the DRMMEsxiHostAudit to represent the network interfaces of the ESXi host being audited.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Ipv4 = 'The IPv4 address of the network interface card (NIC).'
                Ipv6 = 'The IPv6 address of the network interface card (NIC).'
                MacAddress = 'The MAC address of the network interface card (NIC).'
                Name = 'The name of the network interface card (NIC).'
                Speed = 'The speed of the network interface card (NIC).'
                Type = 'The type of the network interface card (NIC).'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a network interface card (NIC) on an ESXi host, including its name, IP addresses, MAC address, speed, and type.'
        }
        DRMMEsxiPhysicalMemory = @{
            Examples = @{
            }
            LongDescription = 'The DRMMEsxiPhysicalMemory class models the information about the physical memory modules of an ESXi host. It includes properties such as Module, Size, Type, Speed, SerialNumber, PartNumber, and Bank, which provide details about each physical memory module installed on the ESXi host. This class is typically used as part of the DRMMEsxiHostAudit to represent the memory configuration of the ESXi host being audited.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Bank = 'The bank location of the physical memory module.'
                Module = 'The identifier or name of the physical memory module.'
                PartNumber = 'The part number of the physical memory module.'
                SerialNumber = 'The serial number of the physical memory module.'
                Size = 'The size of the physical memory module.'
                Speed = 'The speed of the physical memory module.'
                Type = 'The type of the physical memory module.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the physical memory information of an ESXi host, including module, size, type, speed, serial number, part number, and bank.'
        }
        DRMMEsxiProcessor = @{
            Examples = @{
            }
            LongDescription = 'The DRMMEsxiProcessor class models the information about the processor(s) of an ESXi host. It includes properties such as Frequency, Name, and NumberOfCores, which provide details about the CPU configuration of the ESXi host. This class is typically used as part of the DRMMEsxiHostAudit to represent the processor information of the ESXi host being audited.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Frequency = 'The frequency of the processor.'
                Name = 'The name of the processor.'
                NumberOfCores = 'The number of cores in the processor.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the processor information of an ESXi host, including its frequency, name, and number of cores.'
        }
        DRMMEsxiSystemInfo = @{
            Examples = @{
            }
            LongDescription = 'The DRMMEsxiSystemInfo class models the information about the ESXi host system. It includes properties such as Manufacturer, Model, Name, NumberOfSnapshots, and ServiceTag, which provide details about the ESXi host''s hardware and configuration. This class is typically used as part of the DRMMEsxiHostAudit to represent the overall system information of the ESXi host being audited.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Manufacturer = 'The manufacturer of the ESXi host.'
                Model = 'The model of the ESXi host.'
                Name = 'The name of the ESXi host.'
                NumberOfSnapshots = 'The number of snapshots on the ESXi host.'
                ServiceTag = 'The service tag of the ESXi host.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the system information of an ESXi host, including manufacturer, model, name, number of snapshots, and service tag.'
        }
    }
    DRMMFilter = @{
        DRMMFilter = @{
            Examples = @{
            }
            LongDescription = 'The DRMMFilter class models a filter within the DRMM platform, encapsulating properties such as Id, FilterId, Name, Description, Type, Scope, Site (for site-scoped filters), SiteUid, DateCreate, LastUpdated, and PortalUrl. It provides a constructor and a static method to create an instance from API response data. The class also includes methods to determine if the filter is global or site-specific, as well as a method to generate a summary string of the filter''s information. Additionally, it includes methods to retrieve devices and alerts associated with the filter. For site-scoped filters, the Site property provides full context about the associated site, while SiteUid is maintained for backward compatibility.'
            MethodDescriptions = @{
                GetAlerts = @{
                    Description = 'Retrieves the alerts associated with the filter.'
                    Example = $null
                    Parameters = @{
                        Status = 'The status of the alerts to retrieve (e.g., "active", "resolved").'
                    }
                    ReturnValue = 'A list of alerts associated with the filter, optionally filtered by status.'
                }
                GetDeviceCount = @{
                    Description = 'Retrieves the count of devices associated with the filter.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'The count of devices associated with the filter.'
                }
                GetDevices = @{
                    Description = 'Retrieves the devices associated with the filter.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A list of devices associated with the filter.'
                }
                GetSummary = @{
                    Description = 'Generates a summary string for the filter, including its name, scope, and type.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string that includes the filter''s name, scope, and type.'
                }
                IsCustom = @{
                    Description = 'Determines if the filter is a custom type.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A boolean value indicating whether the filter is a custom type.'
                }
                IsDefault = @{
                    Description = 'Determines if the filter is the default type.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A boolean value indicating whether the filter is the default type.'
                }
                IsGlobal = @{
                    Description = 'Determines if the variable is global in scope.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A boolean value indicating whether the filter is global in scope.'
                }
                IsSite = @{
                    Description = 'Determines if the variable is site-specific in scope.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A boolean value indicating whether the filter is site-specific in scope.'
                }
                OpenPortal = @{
                    Description = 'Opens the portal URL associated with the filter in the default web browser.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'This method does not return a value. It performs an action to open the portal URL in the default web browser.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                DateCreate = 'The date and time when the filter was created.'
                Description = 'A brief description of the filter''s purpose or criteria.'
                FilterId = 'The unique identifier of the filter.'
                Id = 'The identifier of the filter.'
                LastUpdated = 'The date and time when the filter was last updated.'
                Name = 'The name of the filter.'
                PortalUrl = 'The URL to access the filter results in the Datto RMM web portal.'
                Scope = 'The scope or context in which the filter is applied.'
                Site = 'The DRMMSite object associated with the filter when it is site-scoped. Provides full site context for site-specific filters.'
                SiteUid = 'The unique identifier of the site associated with the filter.'
                Type = 'The type or category of the filter.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a filter in the DRMM system, including its name, description, type, scope, and associated site.'
        }
    }
    DRMMJob = @{
        DRMMJob = @{
            Examples = @{
            }
            LongDescription = 'The DRMMJob class models a job within the DRMM platform. It includes properties such as Id, Uid, Name, DateCreated, and Status. This class provides methods to interact with job components, results, standard output, and error data. It also includes utility methods to check the job''s status, calculate its age, refresh its data, and generate a summary string. The class is used to represent and manage jobs in the DRMM system.'
            MethodDescriptions = @{
                _Deprecated_GetResults = @{
                    Description = 'Retrieves the results associated with the job for a specific device.'
                    Example = $null
                    Parameters = @{
                        DeviceUid = 'The unique identifier of the device for which to retrieve job results.'
                    }
                    ReturnValue = 'The results associated with the job for the specified device.'
                }
                _Deprecated_GetStdErr = @{
                    Description = 'Retrieves the standard error data associated with the job for a specific device.'
                    Example = $null
                    Parameters = @{
                        DeviceUid = 'The unique identifier of the device for which to retrieve standard error data.'
                    }
                    ReturnValue = 'The standard error data associated with the job for the specified device.'
                }
                _Deprecated_GetStdOut = @{
                    Description = 'Retrieves the standard output data associated with the job for a specific device.'
                    Example = $null
                    Parameters = @{
                        DeviceUid = 'The unique identifier of the device for which to retrieve standard output data.'
                    }
                    ReturnValue = 'The standard output data associated with the job for the specified device.'
                }
                _Deprecated_GetStdOutAsCsv = @{
                    Description = 'Retrieves the standard output data associated with the job for a specific device.'
                    Example = $null
                    Parameters = @{
                        DeviceUid = 'The unique identifier of the device for which to retrieve standard output data.'
                        FirstRowAsHeader = 'Indicates whether the first row of the output should be treated as a header (true/false).'
                        Headers = 'An optional array of headers to use for the CSV output. If not provided, default headers will be used.'
                    }
                    ReturnValue = 'The standard output data associated with the job for the specified device, parsed from CSV into a PowerShell object.'
                }
                _Deprecated_GetStdOutAsJson = @{
                    Description = 'Retrieves the standard output data associated with the job for a specific device.'
                    Example = $null
                    Parameters = @{
                        DeviceUid = 'The unique identifier of the device for which to retrieve standard output data.'
                    }
                    ReturnValue = 'The standard output data associated with the job for the specified device, parsed from JSON into a PowerShell object.'
                }
                _Deprecated_Refresh = @{
                    Description = 'Refreshes the job''s data by fetching the latest information from the API.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'The updated job object with refreshed data.'
                }
                GetAge = @{
                    Description = 'Calculates the age of the job based on its creation date.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'The age of the job as a TimeSpan object, representing the time elapsed since the job was created.'
                }
                GetComponents = @{
                    Description = 'Retrieves the components associated with the job.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A list of components associated with the job.'
                }
                GetSummary = @{
                    Description = 'Generates a summary string for the job.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string representing the job.'
                }
                IsActive = @{
                    Description = 'Checks if the job is currently active.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'Indicates whether the job is currently active (true/false).'
                }
                IsCompleted = @{
                    Description = 'Checks if the job is completed.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'Indicates whether the job is completed (true/false).'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                DateCreated = 'The date and time when the job was created.'
                Id = 'The unique identifier of the job.'
                Name = 'The name of the job.'
                Status = 'The current status of the job.'
                Uid = 'The unique identifier (UID) of the job.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a job in the DRMM system, including its ID, unique identifier, name, creation date, and status.'
        }
        DRMMJobComponent = @{
            Examples = @{
            }
            LongDescription = 'The DRMMJobComponent class models a component within a DRMM job. It includes properties such as Uid, Name, and Variables, which provide details about the component''s identity and configuration. The class also includes a static method to create an instance of DRMMJobComponent from API response data.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Name = 'The name of the job component.'
                Uid = 'The unique identifier (UID) of the job component.'
                Variables = 'The variables associated with the job component.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a component of a DRMM job, including its unique identifier, name, and associated variables.'
        }
        DRMMJobComponentResult = @{
            Examples = @{
            }
            LongDescription = 'The DRMMJobComponentResult class models the result of a component within a DRMM job. It includes properties such as ComponentUid, ComponentName, ComponentStatus, NumberOfWarnings, HasStdOut, and HasStdErr, which provide details about the outcome of the component''s execution. The class also includes a static method to create an instance of DRMMJobComponentResult from API response data.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                ComponentName = 'The name of the job component.'
                ComponentStatus = 'The status of the job component.'
                ComponentUid = 'The unique identifier (UID) of the job component.'
                HasStdErr = 'Indicates if the job component has standard error output.'
                HasStdOut = 'Indicates if the job component has standard output.'
                NumberOfWarnings = 'The number of warnings generated by the job component.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the result of a DRMM job component, including its unique identifier, name, status, number of warnings, and whether it has standard output or error data.'
        }
        DRMMJobComponentVariable = @{
            Examples = @{
            }
            LongDescription = 'The DRMMJobComponentVariable class models a variable within a DRMM job component. It includes properties such as Name and Value, which provide details about the variable''s identity and configuration. The class also includes a static method to create an instance of DRMMJobComponentVariable from API response data.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Name = 'The name of the job component variable.'
                Value = 'The value of the job component variable.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a variable associated with a DRMM job component, including its name and value.'
        }
        DRMMJobResults = @{
            Examples = @{
            }
            LongDescription = 'The DRMMJobResults class models the outcome of a DRMM job execution. It includes properties such as JobUid, DeviceUid, RanOn, JobDeploymentStatus, and an array of ComponentResults, which provide detailed information about the job''s execution and its components. The class also includes a static method to create an instance of DRMMJobResults from API response data.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                ComponentResults = 'The results of the job components.'
                DeviceUid = 'The unique identifier (UID) of the device.'
                HasStdErr = 'Indicates whether any component result produced standard error data.'
                HasStdOut = 'Indicates whether any component result produced standard output data.'
                JobDeploymentStatus = 'The deployment status of the job.'
                JobUid = 'The unique identifier (UID) of the job.'
                RanOn = 'The date and time when the job was run.'
                StdErr = 'The standard error data collected from job components that produced error output.'
                StdOut = 'The standard output data collected from job components that produced output.'
                TotalNumberOfWarnings = 'The total number of warnings across all component results.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the results of a DRMM job, including job and device identifiers, the time the job ran, deployment status, and component results.'
        }
        DRMMJobStdData = @{
            Examples = @{
            }
            LongDescription = 'The DRMMJobStdData class models the standard output or error data produced by a component during the execution of a DRMM job. It includes properties such as JobUid, DeviceUid, ComponentUid, ComponentName, and StdData, which provide details about the source and content of the standard data. The class also includes a static method to create an instance of DRMMJobStdData from API response data.'
            MethodDescriptions = @{
                GetStdDataAsCsv = @{
                    Description = 'Retrieves the standard data associated with a completed job component, parsed from CSV format.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'An array of PSCustomObject instances representing the parsed CSV data, or an empty array if the standard type is not StdOut or the data is empty.'
                }
                GetStdDataAsJson = @{
                    Description = 'Retrieves the standard data associated with a completed job component, parsed from JSON format.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A PSCustomObject representing the parsed JSON data, or null if the standard type is not StdOut or the data is empty.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                ComponentName = 'The name of the job component.'
                ComponentUid = 'The unique identifier (UID) of the job component.'
                DeviceUid = 'The unique identifier (UID) of the device.'
                JobUid = 'The unique identifier (UID) of the job.'
                StdData = 'The standard data output of the job component.'
                StdType = 'The type of standard data, indicating whether it is standard output (StdOut) or standard error (StdErr).'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents standard output or error data associated with a DRMM job component, including job, device, and component identifiers, component name, and the standard data itself.'
        }
    }
    DRMMNetMapping = @{
        DRMMNetMapping = @{
            Examples = @{
            }
            LongDescription = 'The DRMMNetMapping class models a network mapping within the DRMM platform. It includes properties such as Id, Uid, AccountUid, Name, Description, DatatoNetworkingNetworkIds, and PortalUrl. The class provides a constructor and a static method to create an instance from API response data. Additionally, it includes a method to open the portal URL associated with the network mapping in the default web browser. The class serves as a representation of network mappings within the DRMM system, allowing for easy access and management of network mapping information.'
            MethodDescriptions = @{
                OpenPortal = @{
                    Description = 'Opens the portal URL associated with the network mapping in the default web browser.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'This method does not return a value. It performs an action to open the portal URL in the default web browser.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                AccountUid = 'The unique identifier (UID) of the account.'
                DatatoNetworkingNetworkIds = 'The network IDs associated with Datto Networking.'
                Description = 'The description of the network mapping.'
                Id = 'The identifier of the network mapping.'
                Name = 'The name of the network mapping.'
                PortalUrl = 'The URL of the portal.'
                Uid = 'The unique identifier (UID) of the network mapping.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a network mapping in the DRMM system, including properties such as name, unique identifier, description, associated network IDs, and portal URL.'
        }
    }
    DRMMNetworkInterface = @{
        DRMMNetworkInterface = @{
            Examples = @{
            }
            LongDescription = 'The DRMMNetworkInterface class models a network interface card (NIC) associated with a device in the Datto RMM platform. It includes properties such as Instance, Ipv4, Ipv6, MacAddress, and Type, which describe the configuration and identity of the network interface. The class provides a constructor and a static method to create an instance from API response data. This class is used as a property within device audit and device network interface classes to represent individual NICs.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Instance = 'region DRMMNetworkInterface class'
                Ipv4 = 'The IPv4 address of the network interface.'
                Ipv6 = 'The IPv6 address of the network interface.'
                MacAddress = 'The MAC address of the network interface.'
                Type = 'The type of the network interface.'
            }
            RelatedLinks = @()
            ShortDescription = 'region DRMMNetworkInterface class'
        }
    }
    DRMMObject = @{
        DRMMObject = @{
            Examples = @{
            }
            LongDescription = 'The DRMMObject class serves as the base class for all domain model classes in the DattoRMM.Core module. It provides shared utility methods for safely extracting values from API response objects, validating response structures, parsing various API date formats, and masking sensitive string values. All domain classes inherit from DRMMObject to gain access to these foundational capabilities.'
            MethodDescriptions = @{
                GetValue = @{
                    Description = 'Safely retrieves the value of a specified property from a PSCustomObject, returning null if the property does not exist.'
                    Example = $null
                    Parameters = @{
                        InputObject = 'The object from which to retrieve the value.'
                        Key = 'The key or property name whose value is to be retrieved from the input object.'
                    }
                    ReturnValue = 'The value associated with the specified key in the input object, or null if the key does not exist.'
                }
                MaskString = @{
                    Description = 'Masks a string value by replacing characters beyond a specified visible count with a mask character, used to obscure sensitive data such as API keys or secrets.'
                    Example = $null
                    Parameters = @{
                        MaskChar = 'The character to use for masking the string (e.g., "*").'
                        Value = 'The string value to be masked.'
                        VisibleChars = 'The number of characters to leave visible at the start of the string.'
                    }
                    ReturnValue = 'The masked string with the specified number of visible characters at the start.'
                }
                ParseApiDate = @{
                    Description = 'Handle numeric epoch timestamps (int, long, double, or numeric strings)'
                    Example = $null
                    Parameters = @{
                        Value = 'The value to parse as an API date, which can be a numeric epoch timestamp or a date string.'
                    }
                    ReturnValue = 'The DateTime representation of the given API date value, or null if the value cannot be parsed as a date.'
                }
                ValidateShape = @{
                    Description = 'Validates that a PSCustomObject contains all specified required properties, used to verify API response structures before processing.'
                    Example = $null
                    Parameters = @{
                        RequiredProperties = 'An array of property names that are required to be present in the input object.'
                        Sample = 'The sample object to validate against the required properties.'
                    }
                    ReturnValue = 'A boolean value indicating whether the sample object contains all the required properties.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
            }
            RelatedLinks = @()
            ShortDescription = 'region DRMMObject - Base Class'
        }
    }
    DRMMPrinterAudit = @{
        DRMMPrinter = @{
            Examples = @{
            }
            LongDescription = 'The DRMMPrinterAudit class encapsulates detailed information about a printer, such as its unique identifier, portal URL, SNMP information, marker supplies, printer details, system information, and network interfaces. This class is typically used to represent the results of a printer audit operation within the DRMM system.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                PrintedPageCount = 'The total number of pages printed by the printer.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the audit information of a printer, including SNMP info, marker supplies, printer details, system info, and network interfaces.'
        }
        DRMMPrinterAudit = @{
            Examples = @{
            }
            LongDescription = 'The DRMMPrinterAudit class encapsulates detailed information about a printer, such as its unique identifier, portal URL, SNMP information, marker supplies, printer details, system information, and network interfaces. This class is typically used to represent the results of a printer audit operation within the DRMM system.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                DeviceUid = 'The unique identifier (UID) of the device.'
                Nics = 'The network interfaces of the printer.'
                PortalUrl = 'The URL of the portal.'
                Printer = 'The printer associated with the audit.'
                PrinterMarkerSupplies = 'The marker supplies of the printer.'
                SnmpInfo = 'The SNMP information of the printer.'
                SystemInfo = 'The system information of the printer.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the audit information of a printer, including SNMP info, marker supplies, printer details, system info, and network interfaces.'
        }
        DRMMPrinterMarkerSupply = @{
            Examples = @{
            }
            LongDescription = 'The DRMMPrinterMarkerSupply class models the information about the marker supplies of a printer. It includes properties such as Description, MaxCapacity, and SuppliesLevel, which provide details about the printer''s consumable supplies (e'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Description = 'The description of the printer''s marker supply.'
                MaxCapacity = 'The maximum capacity of the printer''s marker supply.'
                SuppliesLevel = 'The current supply level of the printer''s marker supply.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the marker supply information of a printer, including description, maximum capacity, and supply level.'
        }
        DRMMPrinterSnmpInfo = @{
            Examples = @{
            }
            LongDescription = 'The DRMMPrinterSnmpInfo class models the SNMP-related information of a printer. It includes properties such as SnmpName, SnmpContact, SnmpDescription, SnmpLocation, SnmpUptime, NicManufacturer, ObjectId, and SnmpSerial. This class is typically used as part of the DRMMPrinterAudit to represent the SNMP details of the printer.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                NicManufacturer = 'The manufacturer of the network interface card (NIC) used for SNMP communication.'
                ObjectId = 'The SNMP object identifier (OID) associated with the printer.'
                SnmpContact = 'The contact information for SNMP communication with the printer.'
                SnmpDescription = 'The description of the SNMP configuration for the printer.'
                SnmpLocation = 'The physical location of the printer as reported by SNMP.'
                SnmpName = 'The name of the printer as reported by SNMP.'
                SnmpSerial = 'The serial number of the printer as reported by SNMP.'
                SnmpUptime = 'The uptime of the printer as reported by SNMP.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the SNMP information of a printer, including SNMP name, contact, description, location, uptime, NIC manufacturer, object ID, and serial number.'
        }
        DRMMPrinterSystemInfo = @{
            Examples = @{
            }
            LongDescription = 'The DRMMPrinterSystemInfo class models the system-related information of a printer. It includes properties such as Manufacturer and Model. This class is typically used as part of the DRMMPrinterAudit to represent the system details of the printer.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Manufacturer = 'The manufacturer of the printer.'
                Model = 'The model of the printer.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the system information of a printer, including manufacturer and model.'
        }
    }
    DRMMSite = @{
        DRMMDeletedDevicesSite = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDeletedDevicesSite class models a deleted site within the DRMM platform. It includes properties similar to the DRMMSite class, but the Uid property is defined as a string to accommodate cases where the GUID may be invalid or not properly formatted. The class provides a constructor and a static method to create an instance from API response data, allowing for the handling of deleted site information without strict GUID validation.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Uid = 'Shadow the base Uid property with string type to handle invalid GUIDs'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a deleted site in the DRMM system, with properties similar to DRMMSite but with a string type for Uid to handle invalid GUIDs.'
        }
        DRMMDevicesStatus = @{
            Examples = @{
            }
            LongDescription = 'The DRMMDevicesStatus class models the status of devices for a site within the DRMM platform. It includes properties such as NumberOfDevices, NumberOfOnlineDevices, and NumberOfOfflineDevices. The class provides a constructor and a static method to create an instance from API response data. Additionally, it includes a method to generate a summary string of the device status information, providing an overview of the total number of devices and their online/offline status for the site.'
            MethodDescriptions = @{
                GetSummary = @{
                    Description = 'Generates a summary string for the device status, including counts of total devices, online devices, and offline devices.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string that includes the total number of devices, the number of online devices, and the number of offline devices for the site.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                NumberOfDevices = 'The total number of devices.'
                NumberOfOfflineDevices = 'The number of devices that are currently offline.'
                NumberOfOnlineDevices = 'The number of devices that are currently online.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the status of devices associated with a site in the DRMM system, including counts of total devices, online devices, and offline devices.'
        }
        DRMMSite = @{
            Examples = @{
            }
            LongDescription = 'The DRMMSite class models a site within the DRMM platform, encapsulating properties such as Id, Uid, AccountUid, Name, Description, Notes, OnDemand status, SplashtopAutoInstall setting, ProxySettings, DevicesStatus, SiteSettings, Variables, Filters, AutotaskCompanyName, AutotaskCompanyId, and PortalUrl. It provides a constructor and a static method to create an instance from API response data. The class also includes methods to generate a summary string of the site''s information, update site properties, retrieve associated alerts and devices, and open the site''s portal URL in a web browser.'
            MethodDescriptions = @{
                GetAlerts = @{
                    Description = 'Retrieves alerts associated with the site, optionally filtered by status.'
                    Example = $null
                    Parameters = @{
                        Status = 'The status of the alerts to retrieve (e.g., "active", "resolved").'
                    }
                    ReturnValue = 'A collection of alerts associated with the site, optionally filtered by the specified status.'
                }
                GetDeviceCount = @{
                    Description = 'Retrieves the count of devices associated with the site.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'The count of devices associated with the site.'
                }
                GetDevices = @{
                    Description = 'Retrieves devices associated with the site, optionally filtered by a specific filter ID.'
                    Example = $null
                    Parameters = @{
                        FilterId = 'The ID of the filter to apply when retrieving devices.'
                    }
                    ReturnValue = 'A collection of devices associated with the site, optionally filtered by the specified filter ID.'
                }
                GetFilter = @{
                    Description = 'Retrieves a specific filter associated with the site by name.'
                    Example = $null
                    Parameters = @{
                        Name = 'The name of the filter to retrieve.'
                    }
                    ReturnValue = 'The filter associated with the site that matches the specified name, or null if no matching filter is found.'
                }
                GetFilters = @{
                    Description = 'Retrieves filters associated with the site.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A collection of filters associated with the site.'
                }
                GetSettings = @{
                    Description = 'Retrieves the site settings for the site.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'The site settings associated with the site.'
                }
                GetSummary = @{
                    Description = 'Generates a summary string for the site, including its name, unique identifier, and device count.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string that includes the name, unique identifier, and device count for the site.'
                }
                GetVariable = @{
                    Description = 'Retrieves a specific variable associated with the site by name.'
                    Example = $null
                    Parameters = @{
                        Name = 'The name of the variable to retrieve.'
                    }
                    ReturnValue = 'The variable associated with the site that matches the specified name, or null if no matching variable is found.'
                }
                GetVariables = @{
                    Description = 'Retrieves variables associated with the site.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A collection of variables associated with the site.'
                }
                NewVariable = @{
                    Description = 'Creates a new variable associated with the site, with an option to mask the value.'
                    Example = $null
                    Parameters = @{
                        Masked = 'Indicates whether the variable is masked.'
                        Name = 'The name of the variable.'
                        Value = 'The value of the variable.'
                    }
                    ReturnValue = 'The newly created variable associated with the site.'
                }
                OpenPortal = @{
                    Description = 'Opens the portal URL associated with the site in the default web browser.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'This method does not return a value. It performs an action to open the portal URL in the default web browser.'
                }
                RemoveProxy = @{
                    Description = 'Removes the proxy settings for the site.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'This method does not return a value. It performs an action to remove the proxy settings for the site.'
                }
                Set = @{
                    Description = 'Updates the properties of the site based on the provided hashtable of property names and values.'
                    Example = $null
                    Parameters = @{
                        Properties = 'The hashtable of properties to update for the site, where the keys are the property names and the values are the new values to set for those properties.'
                    }
                    ReturnValue = 'This method does not return a value. It performs an action to update the properties of the site based on the provided hashtable of property names and values.'
                }
                SetProxy = @{
                    Description = 'Sets the proxy settings for the site, including authentication credentials.'
                    Example = $null
                    Parameters = @{
                        Password = 'The password for proxy authentication.'
                        Port = 'The port number for the proxy server.'
                        ProxyHost = 'The hostname or IP address of the proxy server.'
                        Type = 'The type of proxy (e.g., HTTP, SOCKS).'
                        Username = 'The username for proxy authentication.'
                    }
                    ReturnValue = 'This method does not return a value. It performs an action to set the proxy settings for the site, including authentication credentials.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                AccountUid = 'The unique identifier (UID) of the account associated with the site.'
                AutotaskCompanyId = 'The identifier of the Autotask company associated with the site.'
                AutotaskCompanyName = 'The name of the Autotask company associated with the site.'
                Description = 'The description of the site.'
                DevicesStatus = 'The status of the devices associated with the site.'
                Filters = 'The filters associated with the site.'
                Id = 'The unique identifier of the site.'
                Name = 'The name of the site.'
                Notes = 'Additional notes about the site.'
                OnDemand = 'Indicates whether the site is on-demand.'
                PortalUrl = 'The URL of the site portal.'
                ProxySettings = 'The proxy settings for the site.'
                SiteSettings = 'The settings for the site.'
                SplashtopAutoInstall = 'Indicates whether Splashtop auto-install is enabled for the site.'
                Uid = 'The unique identifier (UID) of the site.'
                Variables = 'The variables associated with the site.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a site in the DRMM system, including its properties, settings, and associated devices and variables.'
        }
        DRMMSiteGeneralSettings = @{
            Examples = @{
            }
            LongDescription = 'The DRMMSiteGeneralSettings class models the general settings for a site within the DRMM platform. It includes properties such as Name, Uid, Description, and OnDemand status. The class provides a constructor and a static method to create an instance from API response data. Additionally, it includes a method to generate a summary string of the general settings information.'
            MethodDescriptions = @{
                GetSummary = @{
                    Description = 'Generates a summary string for the general settings, including the on-demand status.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string that includes the on-demand status of the site''s general settings.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                Description = 'The description of the site''s general settings.'
                Name = 'The name of the site''s general settings.'
                OnDemand = 'Indicates whether the site is on-demand.'
                Uid = 'The unique identifier (UID) of the site''s general settings.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the general settings for a site in the DRMM system, including properties such as name, unique identifier, description, and on-demand status.'
        }
        DRMMSiteMailRecipient = @{
            Examples = @{
            }
            LongDescription = 'The DRMMSiteMailRecipient class models a mail recipient for site notifications within the DRMM platform. It includes properties such as Name, Email, and Type. The class provides a constructor and a static method to create an instance from API response data. Additionally, it includes a method to generate a summary string of the mail recipient''s information.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Email = 'The email address of the mail recipient.'
                Name = 'The name of the mail recipient.'
                Type = 'The type of the mail recipient (e.g., "To", "Cc", "Bcc").'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a mail recipient for site notifications in the DRMM system, including properties such as name, email, and type.'
        }
        DRMMSiteProxySettings = @{
            Examples = @{
            }
            LongDescription = 'The DRMMSiteProxySettings class models the proxy settings for a site within the DRMM platform. It includes properties such as Host, Port, Type, Username, and Password. The class provides a constructor and a static method to create an instance from API response data. Additionally, it includes a method to generate a summary string of the proxy settings information. The class handles the conversion of password data from the API response, ensuring that it is stored as a secure string when appropriate.'
            MethodDescriptions = @{
                GetSummary = @{
                    Description = 'Generates a summary string for the proxy settings, including the type, host, and port information.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string that includes the type, host, and port information of the site''s proxy settings.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                Host = 'The host address of the proxy server.'
                Password = 'The password for the proxy server.'
                Port = 'The port number of the proxy server.'
                Type = 'The type of the proxy server (e.g., HTTP, SOCKS).'
                Username = 'The username for the proxy server.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the proxy settings for a site in the DRMM system, including properties such as host, port, type, and authentication credentials.'
        }
        DRMMSiteSettings = @{
            Examples = @{
            }
            LongDescription = 'The DRMMSiteSettings class models the overall settings for a site within the DRMM platform. It includes properties such as GeneralSettings, ProxySettings, MailRecipients, and SiteUid. The class provides a constructor and a static method to create an instance from API response data. Additionally, it includes a method to generate a summary string of the site''s settings information, combining details from the general settings, proxy settings, and mail recipients. The class serves as a comprehensive representation of the site''s configuration, allowing for easy access and management of various settings related to the site.'
            MethodDescriptions = @{
                GetSummary = @{
                    Description = 'Generates a summary string for the site''s settings, including on-demand status, proxy information, and mail recipient count.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string that includes the on-demand status, proxy information, and mail recipient count for the site.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                GeneralSettings = 'The general settings of the site.'
                MailRecipients = 'Reuse existing class'
                ProxySettings = 'The proxy settings for the site.'
                SiteUid = 'Reuse existing class'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the overall settings for a site in the DRMM system, including general settings, proxy settings, mail recipients, and site UID.'
        }
    }
    DRMMStatus = @{
        DRMMStatus = @{
            Examples = @{
            }
            LongDescription = 'The DRMMStatus class models the status of the DRMM system, encapsulating properties such as Version, Status, and Started. The class provides a constructor and a static method to create an instance from API response data. The FromAPIMethod static method takes a response object, extracts the relevant information, and populates the properties of the DRMMStatus instance accordingly. The class serves as a representation of the current status of the DRMM system, allowing for easy access to version information, overall status, and the time when the system started.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                Started = 'The start time of the status.'
                Status = 'The current status.'
                Version = 'The version information.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents the status of the DRMM system, including properties such as version, status, and start time.'
        }
    }
    DRMMToken = @{
        DRMMToken = @{
            Examples = @{
            }
            LongDescription = 'The DRMMToken class encapsulates the OAuth token information returned by the Datto RMM authentication endpoint. It includes the access token (stored as a secure string), token type, expiration date, scope, and JWT identifier. This class provides a static method to create an instance from the API response object, ensuring the access token is securely stored and the expires_in value is converted to a DateTime for easier time-based operations.'
            MethodDescriptions = @{
            }
            Notes = @()
            PropertyDescriptions = @{
                AccessToken = 'The OAuth access token, stored as a secure string to protect sensitive credential data.'
                Expires = 'The expiration date and time of the access token, calculated from the token lifetime at the point of creation.'
                Jti = 'The unique JWT identifier for the access token.'
                Scope = 'The OAuth scope granted by the access token.'
                TokenType = 'The type of the access token (e.g., Bearer).'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents an OAuth access token response from the Datto RMM API.'
        }
    }
    DRMMUser = @{
        DRMMUser = @{
            Examples = @{
            }
            LongDescription = 'The DRMMUser class models a user within the DRMM platform, encapsulating properties such as FirstName, LastName, Username, Email, Telephone, Status, Created, LastAccess, and Disabled. The class provides a constructor and a static method to create an instance from API response data. The FromAPIMethod static method takes a response object, extracts the relevant information, and populates the properties of the DRMMUser instance accordingly. The class also includes methods to generate a full name for the user and to provide a summary of the user''s information, including their username and disabled status. The DRMMUser class serves as a representation of users within the DRMM system, allowing for easy access to user information and status details.'
            MethodDescriptions = @{
                GetFullName = @{
                    Description = 'Generates the full name of the user by combining the first name and last name.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'The full name of the user, which is a combination of the first name and last name.'
                }
                GetSummary = @{
                    Description = 'Generates a summary string for the user, including their full name, username, and disabled status.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string that includes the full name, username, and disabled status of the user.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                Created = 'The creation date of the user.'
                Disabled = 'Indicates whether the user is disabled.'
                Email = 'The email address of the user.'
                FirstName = 'The first name of the user.'
                LastAccess = 'The last access date of the user.'
                LastName = 'The last name of the user.'
                Status = 'The current status of the user.'
                Telephone = 'The telephone number of the user.'
                Username = 'The username of the user.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a user in the DRMM system, including properties such as first name, last name, username, email, telephone, status, creation date, last access date, and disabled status.'
        }
    }
    DRMMVariable = @{
        DRMMVariable = @{
            Examples = @{
            }
            LongDescription = 'The DRMMVariable class models a variable within the DRMM platform, encapsulating properties such as Id, Name, Value, Scope, SiteUid, and IsSecret. It provides a constructor and a static method to create an instance from API response data. The class also includes methods to determine if the variable is global or site-specific, as well as a method to generate a summary string of the variable''s information.'
            MethodDescriptions = @{
                GetSummary = @{
                    Description = 'Generates a summary string for the variable, including its name, scope, and value.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'A summary string that includes the name, scope, and value of the variable.'
                }
                IsGlobal = @{
                    Description = 'Determines if the variable is global in scope.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'True if the variable is global in scope; otherwise, false.'
                }
                IsSite = @{
                    Description = 'Determines if the variable is site-specific in scope.'
                    Example = $null
                    Parameters = @{
                    }
                    ReturnValue = 'True if the variable is site-specific in scope; otherwise, false.'
                }
            }
            Notes = @()
            PropertyDescriptions = @{
                Id = 'The unique identifier of the variable.'
                IsSecret = 'Indicates whether the variable is a secret variable.'
                Name = 'The name of the variable.'
                Scope = 'The scope of the variable.'
                SiteUid = 'The unique identifier (UID) of the site associated with the variable.'
                Value = 'The value of the variable.'
            }
            RelatedLinks = @()
            ShortDescription = 'Represents a variable in the DRMM system, including its name, value, scope, and other attributes.'
        }
    }
    Enums = @{
        RMMPlatform = @{
            Examples = @{
            }
            LongDescription = 'The RMMPlatform enum defines the available Datto RMM platform instances. Each value represents a specific regional or deployment platform endpoint identified by its codename. The platform value is used internally to construct API base URLs and portal URLs for the correct Datto RMM instance.'
            Notes = @()
            RelatedLinks = @()
            ShortDescription = 'Defines the available Datto RMM platform instances used for API and portal URL construction.'
            ValueDescriptions = @{
                Concord = 'The Concord platform instance.'
                Merlot = 'The Merlot platform instance.'
                Pinotage = 'The Pinotage platform instance.'
                Syrah = 'The Syrah platform instance.'
                Vidal = 'The Vidal platform instance.'
                Zinfandel = 'The Zinfandel platform instance.'
            }
        }
        RMMScope = @{
            Examples = @{
            }
            LongDescription = 'The RMMScope enum defines the scope levels available within the Datto RMM platform. Scope determines whether a resource such as a variable or filter applies globally across all sites or is restricted to a specific site.'
            Notes = @()
            RelatedLinks = @()
            ShortDescription = 'Defines the scope levels available within the Datto RMM platform.'
            ValueDescriptions = @{
                Global = 'The resource applies globally across all sites in the account.'
                Site = 'The resource is scoped to a specific site.'
            }
        }
        RMMSiteExtendedProperty = @{
            Examples = @{
            }
            LongDescription = 'The RMMSiteExtendedProperty enum defines the types of extended properties that can be requested for a site in the Datto RMM platform. These extended properties allow callers to request additional related data when fetching site information, such as the site''s settings, variables, or filters.'
            Notes = @()
            RelatedLinks = @()
            ShortDescription = 'Defines the extended property types that can be requested when retrieving site information.'
            ValueDescriptions = @{
                Filters = 'Include the filters associated with the site.'
                Settings = 'Include the settings associated with the site.'
                Variables = 'Include the variables associated with the site.'
            }
        }
        RMMThrottleProfile = @{
            Examples = @{
            }
            LongDescription = 'The RMMThrottleProfile enum defines the available API request throttling profiles for the Datto RMM module. Each profile controls the rate at which API requests are sent, balancing between performance and API rate limit compliance. Selecting a more cautious profile reduces the risk of hitting API rate limits at the cost of slower execution.'
            Notes = @()
            RelatedLinks = @()
            ShortDescription = 'Defines the API request throttling profiles for controlling request rate limits.'
            ValueDescriptions = @{
                Aggressive = 'Sends requests at a faster rate with minimal delay between calls, prioritising speed over rate limit safety.'
                Cautious = 'Sends requests at a slower rate with longer delays between calls, prioritising rate limit safety over speed.'
                DefaultProfile = 'The default throttling profile, providing a balanced rate between speed and rate limit compliance.'
                Medium = 'A moderate throttling profile that balances request speed with rate limit awareness.'
            }
        }
    }
}
